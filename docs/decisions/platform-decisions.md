# Registro de Decisões de PLATAFORMA (ADR — P-xxx)

> Decisões **transversais** que valem para todos os produtos. Confirmadas por humano. Mudança passa
> por novo registro. Decisões **específicas de um produto** ficam no decisions-log do produto
> (ex.: `products/doctor-hub/docs/decisions/decisions-log.md`, série D-xxx).

| ID | Decisão | Confirmado em | Notas / impacto |
|----|---------|---------------|-----------------|
| **P-008** | **Doctor-Hub (API + Front) em produção no GCP pessoal + CI/CD simples via GitHub Actions (branch `main`).** Materializa a próxima fatia do strangler-fig (P-006): depois do IdP, sobem **API .NET** e **Front React** no **Cloud Run** (mesmo projeto `portal-tecnologia-500920`, região SP), atrás do **mesmo Load Balancer** do IdP (host-based: `id.` → Keycloak, `api.portaltecnologia.app.br` → API, `doctorhub.app.br` → Front; certs gerenciados separados por SNI). **Banco:** database `doctorhub` **na instância Cloud SQL existente** (isolamento lógico, sem instância nova) via sidecar Cloud SQL Auth Proxy; migrations EF no startup. **CI/CD:** GitHub Actions por repo, push na `main` → test → build → push (Artifact Registry `doctor-hub`) → `gcloud run services update --container`. **Auth do CI = Workload Identity Federation** (keyless, sem chave salva — baseline de segurança). Terraform gerencia a infra; o CI só atualiza a imagem (`ignore_changes`). CORS da API restrito ao front por env. | 2026-07-01 | Confirmado por Alessandro (4 escolhas: reusar Cloud SQL do IdP; front em Cloud Run/nginx; CI via WIF; domínios próprios já; branch `main`). Bootstrap: 1ª imagem buildada/pushada localmente → `terraform apply` → serviços no ar via `*.run.app` (API `/health` 200, `/api/doctors` 401, front servindo, migrations aplicadas). **Pendente (ação do usuário):** registros **A** `api` e `doctorhub` → `136.68.142.130` no registro.br (aí os certs provisionam). **Estende P-006/P-007.** |
| **P-007** | **Esquema de domínios: `portaltecnologia.app.br` = PLATAFORMA (IdP/APIs por subdomínio); `doctorhub.app.br` = SITE (produto Doctor-Hub).** Os serviços de plataforma (transversais) ficam sob `portaltecnologia.app.br` com subdomínios — IdP em **`id.portaltecnologia.app.br`**, API em **`api.portaltecnologia.app.br`** (e demais APIs/produtos por subdomínio). O **site/app do produto** Doctor-Hub fica em **`doctorhub.app.br`**. Materializa no IaC do P-006 (`keycloak_domain`, `front_base_url`). | 2026-06-30 | Confirmado por Alessandro (comprou os 2 domínios; "portaltecnologia p/ APIs e IdPs com subdomínios, doctorhub o site"). Ambos `.app.br` (registro.br). TLS gerenciado pelo Google (Cloud Run domain mapping). **Estende P-006.** |
| **P-006** | **Produção da plataforma começa pelo `portal-identity` (Keycloak) no GCP PESSOAL, via IaC, sem deploy ainda.** Materializa a pendência de produção do **P-003**. **Ambiente:** projeto **pessoal** `portal-tecnologia` (não o projeto da empresa `coronavirus-272213` onde o gcloud está logado) — segue a estratégia "construir pessoal → repassar à empresa com cessão de IP". **Stack de prod** (baseline P-001): **Cloud Run** (Keycloak, `min=max=1`, imagem com providers+tema+`kc.sh build`) + **Cloud SQL Postgres** (persistente, substitui o H2/`start-dev` do DEV) + **Secret Manager** (senhas geradas pelo Terraform) + **Artifact Registry**. Região **southamerica-east1** (LGPD/latência BR). **Realm de prod LIMPO** (sem seed users / sem senha DEV / redirect do domínio real), importado **uma vez**. **Strangler-fig:** só identidade primeiro; doctor-hub api/web em fatias seguintes. **Esqueleto entregue** em `infrastructure/` (Terraform + README) + `services/portal-identity/Dockerfile` — **marcado "não aplicar"**. | 2026-06-30 | Confirmado por Alessandro (2 escolhas: ambiente = **GCP pessoal**; ação = **desenhar plano + IaC sem deploy**). **Pré-condições registradas (não inferir):** 🔴 **OTP real (SMTP/SMS)** antes de depender de código (I-003 é DEV/log); 🟡 **domínio próprio vs `*.run.app`** (define `KC_HOSTNAME`/redirects/realm); 🟡 **cessão de IP** no repasse; 🟢 HA/cache distribuído só quando o tráfego justificar. Terraform **não validado localmente** (binário ausente) — `validate`/`apply` na fatia de deploy. **Estende P-003.** |
| **P-005** | **Renomeação do guarda-chuva: `portal-platform` → `portal-tecnologia`.** O repo oficial de governança da empresa passa a se chamar **`portal-tecnologia`** — a marca da nova plataforma de tecnologia da Portal Telemedicina. Só muda o **nome/identidade**; o conteúdo, o modelo (polyrepo via manifest, P-002/P-004) e os produtos abaixo (doctor-hub, portal-identity, …) seguem idênticos. **Visão de fundo** (não é big-bang): `portal-tecnologia` é a **plataforma greenfield** que absorve os produtos legados (Teleconsulta, Telediagnóstico) **incrementalmente** (strangler-fig), nunca por reescrita total de uma vez — o que já construímos (doctor-hub + portal-identity) é o 1º tijolo dela. | 2026-06-29 | Confirmado por Alessandro ("quero reescrever os projetos da empresa… vamos chamar de portal-tecnologia"; escolheu "renomear o guarda-chuva agora"). **Renomeação LÓGICA** (como o P-002): atualizada a **identidade viva** (`CLAUDE.md`, `README`, `repos.yml`, `Makefile`, arquitetura, READMEs de produto); **histórico preservado** (P-001/P-002/P-004 narram o nome da época). **Símlink** `~/portal-tecnologia` → pasta física (não move a pasta agora, p/ não quebrar serviços rodando). **Pendente (ação sua, externa):** renomear o repo no **GitHub** (`portal-platform`→`portal-tecnologia`) e `git remote set-url origin …/portal-tecnologia.git` (o GitHub mantém redirect do nome antigo). Migração física da pasta + GCP (projeto `portal-tecnologia` já criado no seu GCP pessoal) quando for conveniente. **Estende P-002.** |
| **P-004** | **Os repos próprios da empresa moram FISICAMENTE dentro do `portal-platform` (em `services/<repo>`), não em `~/ptm`. Refina o P-002.** O umbrella é o **repo da empresa**: os produtos comuns e os serviços de plataforma vivem **dentro** dele, em `services/<repo>` — **gitignored** (`/services/` no `.gitignore`), cada um seu **git independente** com remote próprio (NÃO é true-monorepo: as histórias git seguem separadas; o polyrepo do P-002 continua). O manifest `repos.yml` aponta o **path interno** (`path: services/<repo>`), sem `local: ../ptm/...`. Assim já estão `doctor-hub-api`, `doctor-hub-web` e agora `portal-identity`. **Direção:** `teleconsulta` e `telediagnóstico` também migram para dentro — porém são **hubs vivos** (worktrees/helpers), então a migração física é cuidadosa e **não automática** (não mover sem plano). | 2026-06-29 | Confirmado por Alessandro ("não é para colocar dentro do ptm, mas dentro do portal-platform… aqui ficam os produtos comuns, e também teleconsulta e telediagnóstico… esse é o repo umbrella da empresa… vamos adequar"). Corrige a redação do P-002 ("migração só lógica / repos não moram dentro"): a parte **lógica** (linkagem via manifest, sem submodules) vale; a parte **física** passa a ser **dentro do umbrella**. Ajustados: `repos.yml` (header + entry do portal-identity), `CLAUDE.md` raiz (estrutura). `workspace/` segue para clones de repos cujo lar canônico ainda é externo (ex.: teleconsulta em `~/ptm` até migrar). **Estende/refina P-002.** |
| **P-003** | **Plataforma de identidade da empresa em repo próprio `portal-identity` (Keycloak/OIDC), realm único `portal`.** Identidade é **infra transversal** (serve vários produtos, ciclo de vida e dono próprios, fronteira de segurança isolada) → **repo separado**, registrado no manifest `repos.yml` (`kind: infra`, sem `product:`). **Nome vendor-neutral** de propósito (Keycloak = implementação; não `portal-keycloak`); `portal-*` para alinhar ao P-002 (o prefixo `ptm-` foi aposentado). **Modelo:** **realm único `portal`** (uma identidade por pessoa, SSO entre produtos — objetivo de **unificar os doutores**); **cada produto = um client**; papéis do produto = **client roles**. Doctor-Hub: `doctor-hub-web` (público, login OIDC+PKCE) + `doctor-hub-api` (bearer-only, define `admin/demandas/regulacao/gestor`); a API valida o JWT pela audiência e lê `resource_access["doctor-hub-api"].roles`. | 2026-06-29 | Confirmado por Alessandro (3 perguntas: nome=`portal-identity` + URL `git@github.com:PortalTelemedicina/portal-identity.git`; extrair **agora**; realm **único**). Materializa o que nasceu no `doctor-hub-api` (D-140) e **extrai** para o novo repo (`services/portal-identity` — dentro do umbrella, gitignored, git independente; 1º commit feito): `docker-compose` (Keycloak 26 `start-dev --import-realm`, host net :8089, health :9000), `realms/portal-realm.json` (realm como código — fonte da verdade), Makefile (`up/down/status/logs`), README/CLAUDE. Provado: realm `portal`, 2 clients, 4 client roles e 4 usuários-semente mapeados via Admin API. Doctor-Hub limpo (Keycloak saiu do seu compose/.env; `make up` agora só **checa alcance** do IdP). **Pendências (não inferidas):** API valida JWT + web loga via OIDC (slices do Doctor-Hub); **produção** (DB próprio Cloud SQL + modo `start` + IaC; realm-as-code JSON→**Terraform**); **federação/migração de Teleconsulta + Telediagnóstico** (com os donos — unificar os doutores) = P-decision futura; **LGPD** dos usuários reais = decisão legal/produto. **Estende P-002.** |
| **P-002** | **Identidade: `portal-platform` = repo oficial de governança da empresa (Portal Telemedicina); polyrepo via manifest, não submodules.** (1) O hub é **renomeado `ptm-platform` → `portal-platform`** e reposicionado como **guarda-chuva de TODA a empresa** (não só dos produtos hospedados aqui): teleconsulta, telediagnóstico, doctor-hub, dados etc. ficam **abaixo** dele. Remote: `git@github.com:PortalTelemedicina/portal-platform.git`. (2) **Modelo polyrepo**: cada repo é independente; a linkagem oficial é o **manifest [`repos.yml`](../../repos.yml)** + catálogo — **descartados git submodules** (acoplam versão, DX ruim) e **true-monorepo** (juntaria tudo num git). (3) `make workspace` (scripts/workspace.sh) clona/atualiza os repos com `url` confirmada num **`workspace/` gitignored** (visão local de DEV). (4) Catálogo factual: só `portal-platform` e `teleconsulta` têm url verificada; demais marcados `confirmar: true` (não inventar URL — Diretriz Suprema). | 2026-06-23 | Confirmado por Alessandro (criou o repo `PortalTelemedicina/portal-platform`; 2 perguntas: linkagem = manifest+registry; nome = portal-platform governança≠produto; +1 ajuste: teleconsulta/telediagnóstico ficam **abaixo** do portal-platform). **Estende P-001.** |
| **P-001** | **Governança multi-produto: `governança (raiz) + products/ + services/`.** O `ptm-platform` deixa de ser Doctor-Hub-cêntrico e passa a ser **hub de governança da plataforma**. (1) A governança transversal sobe à raiz: `docs/method/` (SDD/TDD/AI-coding), `docs/security/security-baseline.md`, `docs/architecture/platform-architecture.md`, `docs/decisions/platform-decisions.md`, `specs/` (só método + template). (2) Cada produto vira um inquilino em `products/<produto>/` com `CLAUDE.md` próprio, `docs/`, `specs/`, `design/`. (3) **Doctor-Hub** vira o 1º produto (`products/doctor-hub/`), com todo o seu conteúdo (discovery, product, design, business, architecture 00–02, decisions-log D-001..D-110, specs de feature) **movido via `git mv`** (histórico preservado). (4) Onboarding de produto novo via `products/_template/` + registry `products/README.md`. | 2026-06-23 | Confirmado por Alessandro (2 perguntas: foco = camada de governança multi-produto; pode reorganizar de fato). **Estende** D-110 (spec-hub + services) e D-109 (build real/stack), que continuam válidos como história do Doctor-Hub. Stack .NET/React/GCP promovido a **baseline recomendado de plataforma**. |

## Como registrar uma decisão de plataforma
1. A decisão precisa ser **transversal** (afeta mais de um produto ou o hub em si). Se é só de um
   produto, registre no decisions-log daquele produto.
2. Adicione uma linha **P-xxx** acima, com `✅ Confirmado por <humano> em <data>` e o impacto.
3. Se a decisão muda a estrutura, atualize também `CLAUDE.md` (raiz) e
   [`../architecture/platform-architecture.md`](../architecture/platform-architecture.md).
