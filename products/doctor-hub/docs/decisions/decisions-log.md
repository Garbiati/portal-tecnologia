# Registro de Decisões (ADR-lite)

> Decisões de negócio/arquitetura **confirmadas por humano**. Cada uma vira "lei" do projeto.
> Mudança após congelamento passa pelo controle de mudança (a definir na Fase 4 — Estimar).
> Nada aqui foi inferido pela IA — tudo confirmado pelo Alessandro.

> **Nota (protótipo, 2026-06-23):** **login fake + redirecionamento por papel** está CONFIRMADO como comportamento do protótipo — após o login fake, redireciona conforme a persona: **Demandas → Início**; **Regulação → Minhas Solicitações**; **Gestor → Assunção**. **Auth real segue futura** (Identity vs IdP da Portal — decisão própria, D-109); não inferir.

| ID | Decisão | Confirmado em | Notas / impacto |
|----|---------|---------------|-----------------|
| **D-142** | **CONFIRMADA (2026-07-05): matriz papel × ação × escopo.** Após o harness de segurança provar vazamento horizontal (Regulação do cliente A lê/altera dados do cliente B) e falta de RBAC, o Alessandro confirmou: **(1) Escopo:** Regulação vê/age SÓ no seu cliente vinculado; Supervisor SÓ na sua unidade; Demandas e Admin veem tudo (gestão da plataforma). **(2) Base médica** (criar/editar/excluir escala + ficha do médico): só Demandas e Admin. **(3) Solicitações** (criar + "de acordo"): só Regulação, no seu cliente. **(4) Agendamentos** (GET com iniciais de paciente — LGPD): Supervisor só da sua unidade; Demandas/Admin tudo. Admin = superusuário (acessa tudo). Implementação: policies `papel:*` aplicadas aos endpoints + filtro por `clienteId`/`unidade` do token (protocol mapper no Keycloak leva os atributos ao token) + o harness de segurança vira teste que TRAVA. | 2026-07-05 | Fecha os gaps G-1/G-2/G-3 do doc 27. Provado por `homolog-seguranca-e2e.py` (antes: vazava; depois: deve bloquear). |
| **D-156** | **Onboarding self-service do médico + autoproposta de escala com aprovação (IDEIA futura — não desenvolver agora).** Alessandro (2026-07-05): "penso em criar um perfil para o doutor, ele mesmo propor uma escala, um fluxo de aprovação… registrar só a ideia para evolução futura". Além disso, a landing ganha um CTA **"candidate-se"** em estado **"em breve"** com captura de e-mail (notify-me) — hoje só **médicos indicados** participam; o médico se cadastra para ser avisado quando abrir inscrições. Detalhe: [`../product/28-visao-produto-marca-roadmap.md`](../product/28-visao-produto-marca-roadmap.md). | 2026-07-05 | **v0.1 (esta entrega):** landing pública + captação VISUAL do médico ("candidate-se" → "em breve" + avise-me, e-mail NÃO armazenado). **v0.2:** persistir a lista de notify (endpoint + tabela). **Futuro:** perfil do médico, autoproposta de escala, fluxo de aprovação. |
| **D-155** | **Marca = o PRODUTO (doc hub); Portal Telemedicina = case fundador. Visão: hub de soluções médicas com IA.** Direção do Alessandro (2026-07-05): "temos que promover o PRODUTO, não o tenant fundador — ele é um case do produto". Resolve a pergunta aberta do P-009 (marca do produto vs tenant): **promovemos doc hub**; a Portal aparece como prova/case; marca do cliente vive no tema (P-009). Posicionamento: sistema médico que facilita GESTÃO e DISTRIBUIÇÃO da capacidade médica (público/privado). **North-star:** evoluir para um HUB de soluções médicas com IA para decisão/alocação. **Roadmap de produtos futuros** (visão, não priorizado): pareamento médico↔hospital (plantonistas); cálculo de remuneração por serviço (laudo/atendimento/tele/plantão — semente = tipoServico D-150); IA de estratégia/alocação; marketplace médico. Detalhe: [`../product/28-visao-produto-marca-roadmap.md`](../product/28-visao-produto-marca-roadmap.md). | 2026-07-05 | Orienta landing + SEO (D-154/doc 27) e priorização. Perguntas abertas: tagline, modelo comercial, escopo da 1ª landing. |
| **D-154** | **Plano de homologação vivo + gestão de risco de segurança por TESTE NEGATIVO.** Pedido do Alessandro (2026-07-05): plano de homologação (o roteiro manual dele + casos que faltam) e "como gerenciar risco de segurança" (medo nº1: "não errar nas limitações de cada cliente e usuário"). Materializado: [`26-plano-de-homologacao.md`](../product/26-plano-de-homologacao.md) (fases 0–5, cada passo mapeado a um harness E2E) e [`27-seguranca-gestao-de-risco.md`](../product/27-seguranca-gestao-de-risco.md) (modelo de ameaças STRIDE + gaps rastreados + **matriz papel×ação×escopo D-142 a confirmar**). Princípio: risco só é gerido quando é **testado** — para cada limite, um **harness NEGATIVO** (`homolog-seguranca-e2e.py`) prova que o proibido é bloqueado (401/403) no BACKEND, não só escondido na UI. Gaps abertos: G-1 escopo horizontal cliente A×B no backend (ALTO), G-2 RBAC por papel em solicitações/agendamentos, G-3 agendamentos GET global expõe iniciais, G-4 auditoria parcial. | 2026-07-05 | Destrava com a confirmação da matriz D-142 (proposta de partida no doc). Estende D-153 p/ segurança. |
| **D-153** | **Definição de PRONTO = homologação E2E contra infra REAL (não só unit verde).** Após o Alessandro pegar 2 bugs que quebravam a funcionalidade com a suíte unitária 100% verde (foco do Modal I-N/A e vínculo descartado pelo Keycloak I-009), fica a regra: nenhuma tela/feature é "pronta" sem passar um harness E2E que roda os fluxos reais contra API+Keycloak REAIS, afirmando persistência/validação/efeitos. 1º harness: `infrastructure/scripts/homolog-usuarios-e2e.py` (CRUD de usuários + vínculo + validação de CPF + limpeza; passou em prod). Detalhe e padrão p/ as próximas telas: [`../product/25-definicao-de-pronto-e2e.md`](../product/25-definicao-de-pronto-e2e.md). Objetivo: rodar no CI pós-deploy. | 2026-07-05 | Motivo: unit test mocka a API e não pega bug de infra/serialização/RBAC. Reforça a Diretriz de risco nº1 (superconfiança). |
| **D-152** | **Escala FIXA com EXCEÇÃO POR SEMANA DO MÊS ("na 2ª semana do mês não trabalho").** Requisito reafirmado com exemplo concreto (02h29 de 2026-07-05, discovery/13 §1). Modelo: `semanasExcluidas` (1..5) na escala FIXA — nas semanas marcadas o doutor NÃO atende (estoque/painel descontam; conflito INV-1 continua conservador, ignorando exceções). **PROVISÓRIO:** semântica = n-ésima OCORRÊNCIA do dia no mês (2ª semana = 2ª segunda, 2ª terça…, padrão de calendários) — confirmar; quinzenal segue ❓ (ancoragem). FLEX = extensões pontuais ("às vezes segunda até 18h") — papel esclarecido, sem recurso novo (D-151). | 2026-07-05 | Regras de agendamento/falta do doutor (continuidade psiquiatria/psicologia sem troca de médico; doutor-de-plantão da operação; escala de ENCAIXE = reposição; mensagem diferenciada) registradas em discovery/13 — dependem de spec do fluxo de agendamento p/ codificar. |
| **D-151** | **Foco no sistema de escala: FIXA é CERTEZA; FLEX em revisão (congelada).** Alessandro (2026-07-05 madrugada): "vamos focar em um sistema de escala… a fixa é certeza, mas a flex precisamos conversar melhor". Consequência: investimento da noite vai TODO na FIXA (horários por dia — discovery/12 §2 textual, plantão de reposição, indisponibilidade, capacidade real no painel). A FLEX **continua existindo como está** (spec v2/D-120..123, nada é removido) mas **não recebe recurso novo** até a conversa. | 2026-07-05 | Pauta da conversa FLEX: papel dela vs indisponibilidade/reposição; se vira "extra pontual" ou some. |
| **D-150** | **Escala ganha TIPO DE SERVIÇO e vínculo com PROJETO; painel de capacidade passa a ser REAL (objetivo noturno).** Objetivo reafirmado pelo Alessandro (2026-07-05, antes de dormir): "sistema de escalas que atende as necessidades de um projeto, alocando médicos, dando previsibilidade da capacidade médica… valores por atendimento ou por hora… tipo de serviço: laudos, exames, plantão, atendimento, teleatendimento etc". Materializado: (1) **catálogo `tipos_servico`** extensível (seed: teleatendimento [default], atendimento presencial, plantão, laudo, exame — a lista é aberta, "e etc"), GET /api/tipos-servico; (2) **`escala.tipoServico`** (obrigatório, default teleatendimento) e **`escala.clienteId`** OPCIONAL = oferta DEDICADA a um projeto (nulo = pool geral) — é o "alocar médicos às necessidades do projeto"; (3) form de criar escala ganha os 2 selects; cards exibem tipo/projeto; (4) **painel deriva capacidade das escalas REAIS** (fórmula da spec §5 aprovada) por especialidade × tipo de serviço × projeto, com fallback fixture se API fora. **PROVISÓRIO (perguntas p/ o Alessandro):** reserva/prioridade da capacidade dedicada×pool no cruzamento com solicitações; valores por TIPO DE SERVIÇO (hoje valores são por especialidade/laudo — D-125/D-126; estender?); metas do painel. | 2026-07-05 | Ondas noturnas com auto-homologação E2E em prod a cada etapa; relatório matinal no HANDOFF. Valores hora/atendimento já existiam (D-125). |
| **D-149** | **Redesign mobile-first "Dark 9.4" + estrutura de APP (bottom nav · app lists · action sheet · FAB).** Direção visual escolhida pelo Alessandro no artifact de mockups ("dark 9.4" — a paleta do ícone: fundo #04121D, superfícies #081C2B, bordas-filete azuladas #12314A, ÂMBAR #E8763A como cor de AÇÃO no tema escuro; tema claro mantido). Estrutura conforme o guia [`docs/design/mobile-app-patterns.md`](../design/mobile-app-patterns.md) (pesquisa M3/HIG/NN-g/web.dev): **BottomNav** no AppShell (4 primeiros destinos da persona + "Mais"→drawer; safe-area; some ≥768px), telas admin trocam tabela-empilhada por **List/ListItem** (avatar+título+subtítulo+badges; só INATIVO ganha badge de status) com ações no **menu "⋯"→ActionSheet** (sheet no mobile, dropdown no desktop; Excluir destrutiva) e **FAB estendido "+ Novo"** (mobile). Título da página some no mobile (topbar já o exibe). Componentes novos no design system (§16): BottomNav, List/ListItem, ActionSheet, Fab. | 2026-07-05 | Aplicado primeiro em Usuários e Clientes; demais telas nas próximas fatias. Testes: 252 front. Fonte canônica do CSS sincronizada. |
| **D-148** | **Exclusão FÍSICA de usuários e clientes, condicionada à ausência de vínculos.** Regra do Alessandro: "enquanto não tiver um registro vinculado, posso excluir; se tem doutor, escala ou usuário vinculado ao cliente, não posso excluir até excluir todos os vínculos". Materializado: **Cliente** só exclui sem **usuários vinculados** (atributo clienteId no Keycloak) e sem **solicitações** (médicos/escalas não apontam p/ cliente hoje; quando apontarem, entram na checagem); **Usuário** só exclui sem **solicitações/agendamentos criados por ele** (auditoria NÃO bloqueia — trilha preservada) e nunca a si mesmo (anti-lockout). Bloqueio = 409 com a contagem dos vínculos na mensagem. "Excluir" CONVIVE com "Desativar" (D-143 segue o caminho LGPD/histórico). UI: confirmação em modal ("Excluir definitivamente") em Usuários e Clientes; tela de clientes ganhou **modal Detalhes** (todos os campos + id interno + externalId do HC). | 2026-07-04 | Pedido na homologação. Interpretação registrada: vínculos de USUÁRIO = registros de negócio criados (solicitações/agendamentos); auditoria fica. Testes API 59 · front 228. |
| **D-147** | **Clientes viram CADASTRO REAL (Admin) com duas naturezas — projeto PÚBLICO (ex.: "Piauí Saúde Digital") ou cliente PRIVADO (hospital/clínica) — e o USUÁRIO ganha vínculo com cliente/projeto no cadastro.** Decisão do Alessandro (2026-07-04, homologação): "ao criar o perfil que vincula a um projeto, precisa ter a tela de projeto/criação… cadastro de Clientes… público como o Piauí Saúde Digital, ou privado como um hospital ou clínica". Materializado: (1) entidade Cliente ganha `natureza` (publico|privado) + `ativo` (excluir=desativar, padrão D-143) + sigla única; CRUD `/api/clientes` restrito a papel:admin; (2) nova tela **Clientes & Projetos** na área Admin (rota admin-clientes, padrão da tela de Usuários, 10 testes); (3) usuário ganha `clienteId`/`unidade` (atributos no Keycloak via directory) no criar/editar do CRUD de usuários (select dos clientes ativos); (4) **GET /api/me**: o autenticado descobre o próprio vínculo → o AuthGate monta o ESCOPO da jornada (Regulação/Supervisor) a partir do cadastro — o fallback demo C1/SES-PI só vale p/ usuário SEM vínculo (PROVISÓRIO até vincular todos). Multi-papel: SÓ o usuário do Alessandro tem os 4 papéis (confirmado). | 2026-07-04 | Complementa D-143 (CRUD usuários), D-146 (identidade do token) e materializa o "escopo" adiado da SPEC-001. Testes: API 59 · front 223. |
| **D-146** | **Multi-papel NÃO troca a identidade: a persona é SEMPRE o usuário logado (token); a jornada muda só papel/telas/permissões.** Achado do Alessandro na homologação ("não faz sentido logar com meu usuário e ver meu nome como Mariana"). Antes, escolher a jornada carregava a persona da FIXTURE (Demandas→Mariana etc. — muleta da era pré-login-real); agora `personaDoToken(area)` vale para as 4 áreas: nome/avatar/id do token + rótulo do papel escolhido. As personas de demo continuam existindo como LOGINS próprios (mariana/aldair/eronildes — cada uma vê o próprio nome). **PROVISÓRIO (SPEC-001):** o vínculo usuário↔cliente/unidade não existe no cadastro; até lá, Regulação/Supervisor recebem escopo demo (C1/SES-PI · Núcleo Teresina-Centro) — o vínculo real entra com a SPEC-001. | 2026-07-04 | Corrige o comportamento herdado do D-143 (que só tratava o Admin pelo token). Deploy no mesmo dia. |
| **D-145** | **"Ondas do 100%": escala, solicitações, aceite, agendamento e auditoria PERSISTEM no backend (pré-homologação).** Pedido do Alessandro ("preciso das telas 100% p/ hoje; homologar e adequar até segunda"). Materializa no banco de PROD o modelo que o protótipo já validou: (1) **Escalas** (SPEC-MEDICOS-ESCALA v2 — invariantes INV-1..6 como funções puras + 18 testes do Gherkin; effective-dating orquestrado pelo front; INV-3 só na criação de FIXA); (2) **Solicitações + Clientes** (D-103/D-104/D-115; seeds da fixture §1/§5); (3) **Aceite (D-116)** vira FLAG persistida (`aceita`) — não é status novo; (4) **Agendamentos** da Assunção (LGPD: paciente SÓ INICIAIS, payload longo recusado; **`enviadoTc=false` — envio real à TC fica de fora por decisão explícita** ("sem problemas da consulta não entrar de fato no teleconsulta") até a DEP-TC-1); (5) **Auditoria** (D-123) com carimbo quem/quando do token. Front: stores viram cache+persistência otimista (padrão medicos-store); gotchas do raio-x corrigidos (hidratação em /escala e deep-link da ficha; `temEscala` derivado no backend). **PROVISÓRIO explícito:** transições de solicitação sem matriz papel×ação (D-142); vagas da Assunção seguem derivadas (fixture não as enumera); painel/contratação seguem derivados da fixture. | 2026-07-04 | Provado E2E EM PRODUÇÃO (8/8): seed, POST/PATCH solicitação, aceite, escala FIXA criada em médico real, **INV-1 bloqueando conflito (HTTP 400)**, temEscala, agendamento persistido (por: mariana), auditoria. Testes: API 59 · front 213. |
| **D-144** | **Renomeação de rótulo: persona "Gestor" (ex-Gestor Regional) → "Supervisor".** Anotação da sessão de Figma: "Gestor Geral → Regulação" (já era o D-139) e "**Gestor Regional → Supervisor**" (atualiza o D-139, que tinha renomeado para "Gestor"). Alcance: **rótulo de exibição** (seletor de jornada, chip de persona, textos de UI). A **chave técnica permanece `gestor`** (client role no Keycloak, `PapelPersona` interno) — migração de role em produção só com decisão específica. Coerente com o uso na anotação do plantão de reposição ("não fica disponível para os **supervisores** assumir"). | 2026-07-04 | Anotado pelo Alessandro na sessão de validação do Figma (discovery [`12-escala-avancada-e-indisponibilidade.md`](../discovery/12-escala-avancada-e-indisponibilidade.md)). **Supersede parcialmente o D-139.** Novas funcionalidades da mesma sessão (escala avançada, indisponibilidade, plantão de reposição) ficam em DISCOVERY até spec aprovada — perguntas abertas no [`03-open-questions.md`](../discovery/03-open-questions.md). |
| **D-143** | **Área Admin: Início + Gestão de Usuários (CRUD dos papéis) via API → Keycloak.** Spec: [`specs/admin-gestao-usuarios/spec.md`](../../specs/admin-gestao-usuarios/spec.md). O admin gere usuários **pela aplicação** (não pelo console do Keycloak): listar/criar/editar/ativar-desativar + atribuir papéis. **Arquitetura:** `front → doctor-hub-api (RBAC: só papel admin) → Keycloak Admin API`, via **service account** com `realm-management` mínimo — **nunca** front→Keycloak direto. **Regras confirmadas:** **vários papéis por usuário** (dos 4: admin/demandas/regulacao/gestor); **convite por e-mail** (usuário define a própria senha; usa o SMTP do I-005); **"excluir" = desativar** (`enabled=false`, LGPD); **campos nome/e-mail/CPF/telefone todos obrigatórios** (CPF+e-mail únicos, CPF validado); **anti-lockout** (admin não se autodesativa nem tira o próprio papel admin). **Consequência (incluída):** multi-papel exige **seletor de jornada** no login + troca no topbar (revê o "sem troca de persona" do protótipo). **Escopo (vínculo a secretaria/HC) fica fora** (depende da SPEC-001). | 2026-06-30 | Confirmado por Alessandro (6 escolhas: via API; multi-papel; convite e-mail; desativar; todos os campos obrigatórios; seletor de jornada agora). Materializa a pendência "gestão de usuários" do D-140/D-142. Implementação em fatias (TDD): service account no Keycloak → endpoints `/admin/users` na API → área admin no front (`AppShell`, design-system). |
| **D-142** | **API valida o JWT do Keycloak e exige autenticação (slice 2a do auth) — RBAC pronto, matriz fina pendente.** A `doctor-hub-api` virou resource server OIDC: valida emissor+audiência (`doctor-hub-api`) e achata os **client roles** do token (`resource_access["doctor-hub-api"].roles`) em claims de papel. **Linha de base:** `FallbackPolicy = autenticado` → todo endpoint exige login; `/health` é `AllowAnonymous`. Políticas `papel:{admin,demandas,regulacao,gestor}` já existem para uso quando a **matriz "quem pode o quê"** for decidida (🚩 regra de negócio — **não inferida**). | 2026-06-29 | Confirmado por Alessandro ("deixar o doctor-hub o mais produtivo possível… autenticação e autorização… não precisam de alteração"). Impacto: `Auth/KeycloakAuth.cs` (JwtBearer + FallbackPolicy + `KeycloakRolesClaimsTransformation`), `Program.cs` (UseAuthentication/Authorization + `/health` anônimo + **ContentRoot ancorado no assembly** — sem isso o `appsettings.json` com a seção `Auth` não era lido ao lançar a dll de outro cwd), `appsettings.json` (Auth: Authority/MetadataAddress/Audience). Testes: `TestAuthHandler` (esquema de teste por header) + 3 testes (401 sem login, 200 autenticado, /health anônimo) → **31 API verdes**. Realm ganhou **audience mapper** (token do `doctor-hub-web` carrega `aud: doctor-hub-api`). **Provado E2E:** 401 sem token; 200 com token real dos 4 perfis (aldair/regulacao, eronildes/gestor, mariana/demandas, admin-dh/admin). **Pendente:** matriz de permissões por endpoint (sua decisão); slice 2b (web loga via OIDC); gestão de usuários. |
| **D-141** | **Keycloak EXTRAÍDO para o repo de plataforma `portal-identity` (ver P-003); papéis viraram client roles.** O IdP que nasceu dentro do `doctor-hub-api` (D-140) saiu para um repo próprio compartilhado pela empresa, com **realm único `portal`**. Os 4 perfis deixam de ser *realm roles* e viram **client roles** do client resource **`doctor-hub-api`** (admin/demandas/regulacao/gestor); o login é o client público `doctor-hub-web`. A API lerá os papéis de `resource_access["doctor-hub-api"].roles`. Doctor-Hub não hospeda mais o Keycloak (saiu do compose/.env). | 2026-06-29 | Confirmado por Alessandro ("conseguimos separar ele em um projeto aparte?… portal-identity… realm único"). Detalhe e justificativa em **P-003** (`docs/decisions/platform-decisions.md`). Para dev: `make -C services/portal-identity up`. Refina o D-140 (que descrevia o IdP embutido + realm roles). |
| **D-140** | **Auth via Keycloak (IdP/OIDC) — resolve o D-109; perfis = realm roles, agora 4 (com Admin).** O Doctor-Hub passa a delegar identidade/login ao **Keycloak** (open-source, self-hosted): usuários e senhas vivem no Keycloak (não no nosso banco), o **web** loga por OIDC Authorization Code + **PKCE**, e a **API valida o JWT** e aplica **RBAC** pelos papéis do token (`realm_access.roles`). Os perfis viram **realm roles**: **admin · demandas · regulacao · gestor** (Admin é o 4º, pedido agora). | 2026-06-29 | Confirmado por Alessandro (escolheu "scaffold agora" + "conseguimos subir um keycloak?" + "modelo + seed de 4 perfis… falta o admin"). **Slice 1 FEITO (local):** `docker-compose` ganhou serviço `keycloak` (26.0, `start-dev --import-realm`, `network_mode: host` na **:8089** — 8080 ocupada; health :9000); realm **`doctor-hub`** versionado em `services/doctor-hub-api/keycloak/realm-doctor-hub.json` (gitignored) com os 4 roles, client público **`doctor-hub-web`** (PKCE S256, redirect `http://localhost:5174/*`) e **4 usuários-semente** (mariana/demandas, aldair/regulacao, eronildes/gestor, admin-dh/admin; senha **dev-only** `Dev@12345`). Admin do console via `.env` (`KEYCLOAK_ADMIN*`). Provado: discovery OIDC, 4 roles e 4 users via Admin API. **Pendências (não inferidas):** (a) **link usuário→cliente/unidade** (clienteId de Regulação/Gestor) — atributo foi descartado no import (user-profile do KC 26) e o *onde mora* o vínculo é design do slice 2; (b) **API valida JWT + protege endpoints** (slice 2); (c) **web troca login fake/Seletor pelo Keycloak** (slice 2); (d) **produção**: Keycloak com DB próprio + modo `start` persistente, e **conectar a API ao Postgres de produção existente** (string via segredo — você fornece; eu não toco). LGPD (base legal/retenção dos usuários reais) segue pergunta aberta. |
| **D-139** | **Renomeação dos perfis: "Gestor Geral" → "Regulação" e "Gestor Regional" → "Gestor".** Os dois perfis de cliente mudam de nome em todo o sistema. **Antes → depois:** *Gestor Geral* (dá o "de acordo" na disponibilização, dono de Minhas Solicitações/De acordo — ex.: Aldair Moura · SES-PI) → **Regulação**; *Gestor Regional* (assume os slots e monta o agendamento, dono da Assunção — ex.: Eronildes Bastos · Teresina) → **Gestor**. Só muda o **nome do perfil** — papéis, fluxos, rotas, permissões e quem-é-quem das personas seguem idênticos. | 2026-06-29 | Confirmado por Alessandro ("mudar o nome do perfil Gestor Geral para Regulação; e gestor regional para Gestor… altere em todos os documentos"). Alcance escolhido: **rótulos + identificadores**. Impacto (`doctor-hub-web`): type union `PapelPersona` `GestorGeral→Regulacao`, `GestorRegional→Gestor`; ids `P-gestor-geral→P-regulacao`, `P-gestor-regional→P-gestor`; avatares `persona-{regulacao,gestor}.jpg` (git mv); `rotuloPapel` `Regulação`/`Gestor`; helpers de teste + comentários. Docs: substituição em todos os `.md` de `products/`. 208 testes front + `pnpm build` (check:ui+tsc+PWA) verdes. **Mantido como está** (sinalizado): a sigla interna **GG/GR** do contrato do linter de navegação (`23-navegacao-contrato.md`) é rótulo de código do gate, não nome de tela — renomear o contrato fica como decisão à parte. |
| **D-138** | **Ficha do médico PERSISTE no backend (Onda 1 do plano de maturidade).** O que antes vivia só na sessão do front (dados cadastrais, faturamento por especialidade/RQE, faturamento por laudo, status) agora **grava no Postgres** via endpoints de escrita. NOME/CRM seguem **travados** (fonte oficial). O front persiste quando o médico tem `backendId` (veio da API) e cai em **fallback de sessão** para a fixture/offline (demo intacto). | 2026-06-26 | Confirmado por Alessandro ("manda bala"). Impacto backend: `Doctor` (+nascimento/telefone/email), `DoctorEspecialidade` (+modo/valor/tempo/ativo), nova `FaturamentoLaudo`; migration `FichaEditavelEFaturamento`; `DoctorEndpoints` (GET um/lista + PUT dados, PATCH status, PUT/DELETE faturamento esp/laudo); +7 testes (28 no total). Front: `api.ts` (DTO+escrita), `medicos-store` (`persistir*`), wiring de DadosMedico/ZonaDeRisco/faturamento; 208 testes. **Provado E2E:** edição pelo UI → Postgres (valor 180→200). Concorrência = last-write-wins (🚩 pergunta aberta). |
| **D-137** | **Tema claro/escuro/sistema** (1ª entrega do gap de Conta da auditoria Figma — `figma-app-audit.md` #26). Preferência **funcional e persistida** (localStorage, por dispositivo); `sistema` segue o SO (`prefers-color-scheme`) e reaplica ao vivo. Modo escuro feito **só remapeando os tokens semânticos** (`[data-theme="dark"]` em `tokens.css`) — fills da marca (accent/cta) intactos; contrastes **AA** conferidos. Controle segmentado (Claro/Escuro/Sistema) em Configurações · Preferências. | 2026-06-26 | Confirmado por Alessandro ("comece pelo tema claro/escuro"). Impacto: `tokens.css` (bloco dark), `src/app/theme-store.ts` (novo), `index.html` (script anti-flash), `main.tsx` (`iniciarTema`), `configuracoes` (controle). +6 testes (207 verdes). **Pendentes do mesmo gap** (não inferidos): idioma/densidade, Conta&Segurança (auth D-109), Privacidade&LGPD (backend). |
| **D-136** | **Remover "+ Novo médico" (cadastro local) da lista de Médicos.** Hoje o cadastro de médico **nasce na Teleconsulta** (sync) — criar um médico localmente na sessão não é coerente com isso. O botão + modal de cadastro saem agora. **Quando o cadastro inverter para o Doctor-Hub** (passa a ser a fonte da verdade), o cadastro/gestão de médicos será **redesenhado então**, com funcionalidades novas a definir — ex.: **anexo de documentos, contrato**, etc. (não inferir agora — vira escopo futuro). | 2026-06-26 | Confirmado por Alessandro ("o que tem hoje não está correto"). Impacto: `medicos-escala/index.tsx` (remove botão/modal `NovoMedicoModal`), `medicos-store.ts` (remove `adicionarMedico`), teste do fluxo removido. 202 testes front verdes. Ver pergunta aberta "Cadastro/gestão de médicos no Doctor-Hub (pós-inversão)". |
| **D-135** | **Ficha do doutor mostra as especialidades REAIS do sync (Figma "Ficha ver").** A tabela "Teleconsulta · por especialidade" deixou de ser alimentada só pela config de faturamento — agora **lista as especialidades reais do doutor** (vindas do sync) mescladas com RQE + modo/valor/tempo (mostra "—" quando não configurado) e ação **"Configurar faturamento"**. "+ Especialidade" = **vincular outra** especialidade. Header ganha sub-linha **"Ref. Teleconsulta {externalId} · Criado · Atualizado"** (datas do sync). Card **Provisionamento** mostra o **vínculo REAL de origem** (o doutor já existe na TC) — **NÃO inventa status "provisionado"** (seria dado falso — Diretriz Suprema); texto mais claro ("pronto para faturar" / "faturamento não configurado"). | 2026-06-26 | Confirmado por Alessandro (escolheu "Seguir o Figma" + ajustar header/estados-vazios/provisionamento). Impacto: API `/api/doctors` devolve `especialidades:[{nome,rqe,principal}]` + `criadoEm/atualizadoEm`; front `Medico.especialidadesRqe`/`externalId`; `faturamento.tsx` (`mesclarEspecialidades` + upsert por nome, +4 testes); `cards.tsx`/`index.tsx`. RQE do sync vem vazio (origem não tem — ver pergunta aberta) → preenchível aqui. 203 testes front + 21 api verdes. |
| **D-134** | **API roda contra Postgres REAL e persistente (carga D-133 materializada).** A `doctor-hub-api` deixou de depender do modo demo in-memory: aplica **migrations no startup** e faz a **carga inicial dos doutores** (flag `Seed:Doctors`) pelo MESMO `DoctorSyncService` — carga e sync futuro compartilham o upsert idempotente. O **Postgres sobe via `docker-compose`** com **`network_mode: host` na porta 5440** — decisão de ambiente: o NAT de porta publicada do Docker (`-p`) **não encaminha tráfego de forma confiável** nesta máquina (host→container falha, inclusive o handshake do Postgres); host networking resolve e 5440 evita colidir com o Postgres nativo na 5432. O sync ganhou **higiene de CPF** (só dígitos; aceita 11, senão `null`) porque a TC tem CPF sujo (zeros 15–17 chars) que estoura o `varchar(14)`. | 2026-06-26 | Confirmado por Alessandro ("não consegue colocar tudo em docker?"). Impacto: `Program.cs` (`InicializarBancoAsync` — migrate/EnsureCreated + seed desacoplado + guarda defensiva p/ `/health` sem banco), `DoctorSyncService.NormalizarCpf` (+4 testes), `docker-compose.yml`, `.env.example`. Provado: 4523 doutores carregados e **persistidos** no PG real, servidos por `/api/doctors` → proxy do Vite. **NÃO valida dígito verificador de CPF** (seria regra de negócio — pergunta aberta). API ainda roda como **processo no host** em dev (rebuilds rápidos); containerizar a API = questão de deploy (Cloud Run), não necessário agora. |
| **D-133** | **Backend iniciado: Doctor-Hub vira a FONTE DA VERDADE de médicos.** Hoje o cadastro nasce na Teleconsulta; criamos um **sync TEMPORÁRIO e TOTALMENTE DESACOPLADO** que faz a **carga inicial** + **rotina de novos/atualizações**. Fonte = **BANCO da TC, PULL read-only (D-069)** — `doctor_profiles` + joins, incremental por `updated_at`/`deleted_at`. Agendador = **BackgroundService leve (PeriodicTimer)** (sem Hangfire — temporário/removível). Upsert **idempotente** por (Origem, ExternalId); removido na TC ⇒ **desativa** aqui (soft, não apaga). **Quando o cadastro centralizar aqui, o sync é removido e o cadastro/gestão de médicos sai dos produtos.** | 2026-06-25 | Confirmado por Alessandro. Impacto: `services/doctor-hub-api` — entidade `Doctor` (Id/ExternalId/Origem) + migration; módulo `Integration/Teleconsulta` (IFonteDoutoresTeleconsulta + DoctorSyncService). **Abertos:** mapeamento exato TC→Doctor (confirmar c/ humano, baseline); política de conflito na transição; persistência do watermark; credencial read-only dedicada (infra). |
| **D-132** | **Navegação separa "Médicos" e "Escala" (Figma nav: Início/Médicos/Escala/Demandas).** **Médicos** = lista cadastral (abre a página do médico). **Escala** = tela própria (Figma "Escala · Localizar/Ativo"): localizar o doutor → gerir a escala dele (capacidade, FIXA/FLEX, histórico, linha do tempo) + "Trocar médico" + "Ver cadastro do médico". A gestão **reusa o MESMO componente `PerfilEscalas`** da página do médico — **1 gestão canônica de escala, 2 pontos de entrada** (sem tela divergente, preserva o espírito do D-108). | 2026-06-25 | Confirmado por Alessandro (escolheu "tela de Escala própria, 100% Figma"). Impacto: rota/slug `escala` + `src/pages/escala/`; `PerfilEscalas` exportado; nav renumerada em `routes.ts`. A escala continua acessível também pela página do médico. |
| **D-131** | **Ciclo de vida da escala (Figma):** escala ATIVA tem ações **Arquivar** (reversível — para de gerar slots; confirmação leve, sem senha, auditada) + **Linha do tempo**. **Histórico** lista as arquivadas com Início/Encerrada em/Total previsto; **Reativar** só a **última** arquivada (determinística via `arquivSeq`) e **bloqueado se conflitar (INV-1)** com uma ativa **ou criar 2ª FIXA ativa (INV-2)**; **Excluir** (definitivo) mantém **D-123** (escala que já teve vigência exige **senha de gestão**; futura → leve). **Troca de FIXA (INV-2)** passa a **arquivar** a anterior (vai pro histórico/timeline). **Presets** de horário: Meia manhã/Meia tarde/Integral c/ almoço/Personalizado (**Madrugada cruza meia-noite → desabilitado**, motor não calcula, D-022 futuro). **Modal Linha do tempo** plota as vigências (ativa × arquivada). **Médico inativo ⇒ edição TOTALMENTE bloqueada** (Editar dados, faturamento, criar/arquivar/reativar/excluir escala) e a **capacidade da página zera** (coerente com o consolidado, que já exclui inativos) — só sobra Reativar o médico. | 2026-06-25 | Confirmado por Alessandro ("até ficar o mais completo possível"). Reforços vieram de revisão adversarial (coerência inativo×KPI, INV-2 no reativar, determinismo da "última"). Reconcilia Figma (confirmações inline + arquivar) com D-123 (senha p/ destrutivo iniciado): arquivar=reversível leve, excluir=destrutivo mantém senha. Impacto: `encerradaEm` em EscalaMedica; `escalasAtivas/escalasArquivadas/estoquePrevistoTotal`; `comEscalasDoStore` agora traz TODAS as escalas (status filtrado nos consumidores); UI em `medico-detalhe`. |
| **D-130** | **Cards da ficha do médico (Figma "Ficha ver"):** (1) **Provisionamento · destinos** — Teleconsulta/Telediagnóstico com status derivado do faturamento configurado, marcado **"provisório — mecanismo a definir"** (sem ação — integração ao produto = questão aberta); (2) **Histórico de alterações** — alimentado pela **trilha de auditoria REAL** filtrada por médico (`alvoId`), com diffs "Campo: antes → depois" nas edições cadastrais (não dados fake); (3) **Histórico de atendimentos · por vigência** — **Previstos** derivados da escala (estoque), **Realizados = 0** (integração de atendimentos futura); (4) **Zona de risco — Inativar/Reativar médico** (confirmação + auditado; inativo ⇒ edição bloqueada). | 2026-06-25 | Confirmado por Alessandro (análise Figma×app). Impacto: `alvoId` em `RegistroAuditoria`; `cards.tsx` na página `/medico/:id`. **Aberto:** mecanismo de provisionamento aos produtos de destino. |
| **D-129** | **Código da escala ALINHADO à D-039: input = "Duração (min)/consulta", não "consultas/hora".** `EscalaMedica.consultasPorHora` → **`duracaoMin`**; vagas/dia = Σ minutos dos blocos ÷ duracaoMin (consultas/hora vira **derivado**: 60÷duracaoMin). Bate com o Figma "Nova escala". **Números canônicos preservados** (todas as escalas eram 3/h = **20 min**; 132 fixa / ≈156 efetivo idênticos). | 2026-06-25 | Confirmado por Alessandro (análise Figma×app). A D-039 já decidia isso; aqui o **código** foi corrigido (estava em consultas/hora). Impacto: `types.ts`, `escala.ts` (`consultasPorHora()` derivada + `vagasPorDiaEscala`), `fixtures.ts`, `CriarEscala`/`EscalaCard`, testes. |
| **D-128** | **CRUD de faturamento na ficha do médico (Figma "Ficha editar").** Cada especialidade (Teleconsulta) e exame/modalidade (Telediagnóstico) pode ser **adicionado**, **alterado** (o campo-chave — especialidade/exame — fica **travado**: "para trocar, exclua e adicione"), **inativado/reativado** (deixa de faturar sem apagar) e **removido** (com confirmação 2-cliques). Tudo **auditado**. Só **especialidades ATIVAS** contam para "cadastro completo para faturamento" (D-125). Médico **inativo ⇒ edição bloqueada**. | 2026-06-25 | Confirmado por Alessandro (análise Figma×app: "funcionalidades, campos"). Substitui o PROVISÓRIO de D-127 (edição de valores). Impacto: `ativo?` em `FaturamentoEspecialidade`/`FaturamentoLaudo`; `examesCatalogo` (fixtures, DEMO); `FaturamentoMedico` (src/pages/medico-detalhe/faturamento.tsx). **Aberto:** effective-dating (a partir de quando o novo valor vale) — hoje vale imediato. |
| **D-127** | **Botão "Editar" no card "Dados do médico" é FUNCIONAL (sem clique morto).** Edita os campos cadastrais **CPF, nascimento, telefone, e-mail** (com máscaras do design system); **NOME e CRM ficam BLOQUEADOS** — vêm do credenciamento/fonte oficial (cadeados no Figma). Salvar grava no store, atualiza "Atualizado" e fica **auditado** ("editou dados do médico"). **PROVISÓRIO:** edição dos **valores de faturamento** (especialidade/laudo) ainda **não** está no escopo deste editor — questão aberta. | 2026-06-25 | Confirmado por Alessandro (Figma tem "Editar" no card). Impacto: `atualizarMedico` (medicos-store) + `EditarDadosModal` na página `/medico/:id`. |
| **D-126** | **Telediagnóstico é um 2º PRODUTO (além da Teleconsulta).** O médico também pode faturar **TELEDIAGNÓSTICO**, cobrado **por LAUDO** (tipo de exame → **valor/laudo**, ex.: Ecocardiograma R$ 25, ECG R$ 12). É um produto distinto da Teleconsulta (que é por consulta/hora). Cada produto tem seu provisionamento (destino) e, futuramente, sua própria capacidade (laudos × tempo) — **modelo de capacidade do telediagnóstico = questão aberta** (a escala atual modela consultas; laudos serão tratados depois). | 2026-06-25 | Confirmado por Alessandro (Figma "Dados do médico" atualizado nesta sessão). Impacto: `Medico.faturamentoLaudos` + seção na página do médico. Provisionamento e capacidade por produto = estágios seguintes. |
| **D-125** | **Cadastro de FATURAMENTO do médico (Teleconsulta, por especialidade).** Cada especialidade habilitada tem **RQE** (registro de especialista), **modo** de cobrança (**por consulta** ou **por hora**), **valor** (R$) e **tempo/atend.** (min, no modo consulta). Ex.: Cardiologia · RQE 12345 · por consulta · R$ 40 · 20 min; Clínica geral · por hora · R$ 180. **"Cadastro completo para faturamento"** exige **CPF + ≥1 especialidade com valor + ≥1 especialidade com RQE** — sem isso o médico não fatura. | 2026-06-25 | Confirmado por Alessandro (Figma "Dados do médico" atualizado nesta sessão). Impacto: `Medico` ganha cpf/nascimento/telefone/email + `faturamentoEspecialidades`; helper `cadastroCompletoFaturamento` (src/data/faturamento.ts); seção "Dados do médico" na página `/medico/:id`. |
| **D-124** | **A trilha de auditoria é SEMPRE-ON — não desligável pelo usuário.** Não existe feature flag para ligar/desligar a auditoria: toda ação sensível (excluir/trocar escala, disparo à captação, assunção de vaga, mudança de configuração) é registrada incondicionalmente. Um interruptor que permitisse parar a trilha seria um buraco de auditoria (alguém desliga e age sem rastro) — inaceitável em healthcare/LGPD. As feature flags existem apenas para funcionalidades de produto (ex.: disparo à captação, escalas FLEX), nunca para a observabilidade. | 2026-06-25 | Decidido na 3ª auditoria de maturidade (consenso dos 3 agentes). Impacto: removida a flag `auditoria` do config-store e da tela "Configurações do sistema"; `auditar()` sempre grava. |
| **D-123** | **Excluir escala: livre se futura; senha de GESTÃO (auditada) se já iniciada.** A escala pode ser criada e excluída **livremente enquanto a data de início estiver no futuro** (ainda não começou). Se a escala **já iniciou**, a exclusão exige uma **senha de gestão** — funcionalidade do perfil **Demandas Médicas** (não é admin) para exclusões/alterações sensíveis; **senha FIXA por enquanto** (constante de protótipo; auth real depois). UX: botão "Excluir" com **ícone de cadeado** → modal pede a senha → libera. **Toda operação é AUDITÁVEL** (quem fez o quê, com quem, quando), **principalmente** quando a senha é solicitada. Caso de uso: FLEX criada p/ daqui 1h, doutor não apareceu, passaram 2h → excluir a escala não usada. | 2026-06-24 | Confirmado por Alessandro. Impacto: modal de senha + trilha de auditoria nas operações de escala. Spec `medicos-escala` §6. |
| **D-122** | **FLEX é por HORA e SOMA capacidade na sua vigência; escala tem 1..N blocos/dia.** Granularidade: **FIXA por dia** (data), **FLEX por hora** (data+hora — ex.: "4h hoje à noite 19–23h"). No período de vigência da FLEX, suas vagas **somam** às da FIXA (a escala efetiva = fixa+flex). Uma escala pode ter **vários blocos de horário no mesmo dia** (ex.: 08–12 **e** 13–17, intervalo de almoço) — alguns médicos fazem almoço, outros não. **Sem limite de horas/dia** por doutor nesta entrega (🟡 revisitar). | 2026-06-24 | Confirmado por Alessandro. Estende o cálculo de estoque (D-112) para **multi-escala/multi-bloco**: `Σ escalas vigentes [ Σ blocos (horas × consultas/hora) × dias válidos ]`. Spec `medicos-escala` §5. |
| **D-121** | **FIXA é permanente e IMUTÁVEL (effective-dating); regra de SEM SOBREPOSIÇÃO cross-especialidade.** A FIXA tem início e **sem data de fim**; **não se edita** — para alterar, **encerra a atual** (data-fim, default hoje) e **cria uma nova** que **nunca retroage** (início **≥ amanhã**, default amanhã; pode-se programar p/ o futuro e até deixar intervalo entre escalas). **Sem sobreposição:** nenhum par de escalas do mesmo médico ocupa o **mesmo slot** (dia+horário) com vigências cruzando — vale entre **quaisquer** escalas, **inclusive especialidades diferentes** (o médico não está em 2 lugares ao mesmo tempo; pode ser Clínica geral no sáb e Cardio no dom, mas não no mesmo slot). | 2026-06-24 | Confirmado por Alessandro. Preserva histórico/auditoria (não reescreve o passado). Invariantes INV-1..INV-3 da spec `medicos-escala` §4 — núcleo crítico cercado de teste. |
| **D-120** | **Escala multi-tipo: 1 FIXA + N FLEX; a escala efetiva do médico = FIXA + Σ FLEX.** Substitui o modelo de "uma escala fixa por médico". Um médico tem **no máx. 1 FIXA ativa** (contrato CLT, a oferta permanente) **+ 0..N FLEX** (capacidade temporária: hora extra, plantão, cobrir gap/falta). Combos: `1 fixa` (ideal) · `1 fixa+N flex` · `0 fixa+N flex` · `0 fixa+1 flex` (**médico temporário** = só flex). A escala pertence a **uma especialidade do médico** (que pode ter N especialidades). **Precisamos da visão CONSOLIDADA** (fixa+flex) = capacidade estratégica. | 2026-06-24 | Confirmado por Alessandro (sessão de discovery 2026-06-24). Atualiza a spec `medicos-escala` (v2) — antes era escala fixa única. Reabre o modelo de dados: `Medico.especialidades[]` + `Escala[]` (tipo/dias/blocos/vigência). A produtividade real será medida pelos atendimentos; a escala dá a visão de capacidade. |
| **D-119** | **Demo alinhado ao Piauí: o caso coberto (UC-COBRE) passa de SES-AP para SES-PI** para tornar o pipeline ④Assunção/⑤Agendamento alcançável de ponta a ponta no protótipo. Agora **SES-PI tem 2 solicitações de Cardiologia** (D-103 permite várias por cliente): UC-COBRE (200, coberta/RESERVADA → Regulação dá "de acordo" → Gestor assume → **busca de doutor**) e UC-FALTA (1000, faltam 300, narrativa "em falta" intacta). | 2026-06-24 | Confirmado por Alessandro ("alinhar tudo no Piauí"). Resolve furo de coerência: as vagas de assunção dependiam de um "de acordo" de SES-AP, mas não há persona Regulação de SES-AP (as personas logáveis são SES-PI). Impacto: `sobrepor-capacidade` vira "SES-PI (com capacidade)"; roteamento do Inbox passa a ser por COBERTURA (não por cliente). UC-FALTA, home, sobrepor-falta inalterados. SES-AP segue como cliente no catálogo (clientes-hcs), sem cenário ativo. |
| **D-118** | **Flag ">30 dias" nas telas de Disponibilização/Sobrepor = DESLIGADA por ora (regra indefinida).** A sinalização ">30 dias" (que apareceria nas telas de Disponibilização/Sobrepor) fica **desativada** nesta entrega — a regra (prazo, gatilho, efeito) **ainda não está definida**. Vira **pergunta aberta**; não inferir. | 2026-06-23 | Confirmado por Alessandro. Não exibir a flag até a regra ser confirmada. Registrada em `docs/discovery/03-open-questions.md` (flag ">30 dias"). |
| **D-117** | **Capacidade "disponível" (alocável na janela) ≠ "instalada".** A capacidade **disponível** (o que é efetivamente alocável dentro da janela da solicitação) é **distinta** da capacidade **instalada** (total). No demo, **só Cardiologia é gargalo**: disponível **700** de **7.000** instalada — o que **sustenta o UC-FALTA "faltam 300"** (coerente com a contratação). A **regra de ATRIBUIÇÃO/decremento** da capacidade entre clientes simultâneos (como a disponível é consumida quando vários clientes reservam ao mesmo tempo) fica **PROVISÓRIA / indefinida** — não inferir. | 2026-06-23 | Confirmado por Alessandro. Refina D-101 (capacidade total × total solicitado): introduz o eixo **disponível na janela**. Demais especialidades: disponível = instalada (sem gargalo no demo). A regra de atribuição entre clientes simultâneos vira pergunta aberta em `docs/discovery/03-open-questions.md`. |
| **D-116** | **Ação "De acordo" (Regulação) marca a solicitação como ACEITA pelo cliente e LIBERA a etapa de Assunção.** Quando o **Regulação** dá **"De acordo"** na disponibilização (Entregue/Provisionado), a solicitação passa a **ACEITA pelo cliente** e isso **libera a etapa de Assunção** — o **Gestor** passa a poder **assumir** os slots (montar o agendamento). | 2026-06-23 | Confirmado por Alessandro. Concretiza o downstream de D-097/D-098 (aceite do Regulação → Gestor assume → agendamento). Define o **gatilho** que destrava a Assunção: é o "De acordo", não a notificação. |
| **D-115** | **Solicitação carrega janela "A PARTIR DE → ATÉ" (estende D-103); datas de exemplo nos 6 use cases.** Reafirma que a solicitação tem uma **janela editável** ("A PARTIR DE" + "ATÉ", default até o fim do mês — D-103) e **adiciona datas de exemplo** aos **6 use cases** da demo (UC-COBRE/UC-FALTA/UC-RESERVAR/UC-VOLTAR/UC-CANCELAR/UC-ENTREGUE). São **só preenchimento de demo, não regra nova**. | 2026-06-23 | Confirmado por Alessandro. Estende D-103 (janela A PARTIR DE → ATÉ). Datas aplicadas na fixture canônica (`docs/product/22-demo-fixtures.md` §5): UC-COBRE 24/06→02/07, UC-FALTA 20/06→30/06, UC-RESERVAR 22/06→26/06, UC-VOLTAR 18/06→28/06, UC-CANCELAR 08/06→16/06 (expirada), UC-ENTREGUE 19/06→02/07. |
| **D-114** | **Cliente = 1 ponto na 1ª entrega (SEM nível de Health Center).** Na 1ª entrega o **cliente é um único ponto** — **não há** o nível intermediário de **Health Center (HC)**. Efeitos: (1) a tela **"Clientes & HCs"** vira **lista de clientes** (sem desdobrar HCs); (2) a **"Nova solicitação"** **não escolhe unidade/HC**. Os **HCs ficam para depois**. | 2026-06-23 | Confirmado por Alessandro. Simplifica o modelo de 3 níveis (D-031/D-038, que assumia estado→unidade) para a 1ª entrega: **cliente plano**. Impacto Figma/app: renomear/simplificar "Clientes & HCs" → lista de clientes; remover seletor de unidade da Nova solicitação. HC volta como evolução futura. |
| **D-113** | **Período do cálculo de estoque (demo) = MÊS CORRENTE (junho/2026, 22 dias úteis).** Para a demo, o cálculo do estoque de vagas (D-005/D-112) usa o **mês corrente — junho/2026, 22 dias úteis** — como `dias_válidos_no_período`. Ex.: **Dr. Henrique** = `4h × 3 consultas/h = 12/dia × 22 dias úteis = **264 vagas/junho**`. | 2026-06-23 | Confirmado por Alessandro. Fixa o **período** que faltava em D-112 (a fórmula da base pura): para a demo, é o mês corrente (junho/2026 · 22 dias úteis). Aplicado na fixture canônica (`docs/product/22-demo-fixtures.md`): Henrique = 264. (Janelas reais por solicitação seguem D-103/D-115; este período é o do **estoque** do médico no mês.) **⚠️ SUPERSEDIDO por D-120..D-122 (2026-06-24):** o modelo v2 (FIXA+ΣFLEX com effective-dating) recalcula Henrique = **156** (FIXA 132 = 12/dia × 11 dias úteis a partir de 16/06 + FLEX 24) e capacidade Cardiologia = 168. O período canônico (22 dias úteis fixos × mês∩vigência) é **questão aberta** (`docs/discovery/03-open-questions.md`). Não usar 264 como valor atual. |
| **D-112** | **Fórmula da base calculada do estoque = BASE PURA (sem descontos).** A base calculada do estoque de vagas (D-005) é `(horas_no_dia × consultas_por_hora) × dias_válidos_no_período`, onde `horas_no_dia = horário_fim − horário_início` e `dias_válidos = nº de ocorrências dos dias da escala dentro de [período_início, período_fim]`. **Não descontar** intervalo de almoço nem feriados nesta entrega (consultas/hora já embute a duração da consulta). Refinamentos (almoço/feriados) ficam para depois, se necessário. | 2026-06-23 | Confirmado por Alessandro (pergunta da spec `medicos-escala` §8 🔴-1). Desbloqueia o cenário de cálculo de estoque. Parâmetros do edital (≈3 consultas/hora, plantão mín. 4h) seguem como **referência**, não default fixo. |
| **D-111** | **Granularidade do estoque = CONTAGEM (inteiro).** A vaga é uma **contagem de capacidade** (um inteiro por médico/especialidade/período), **não** um horário concreto (slots datados) nesta entrega. Horário concreto, se necessário, fica para a etapa de Assunção/agendamento. Define o modelo de dados do estoque. | 2026-06-23 | Confirmado por Alessandro (pergunta da spec `medicos-escala` §8 🔴-2). Casa com a recomendação do `03-open-questions.md` de começar como contagem. |
| **D-110** | **Estrutura `ptm-platform` (spec-hub + services) — guarda-chuva da empresa.** Criado `~/ptm/ptm-platform` como **spec-hub** da plataforma interna (análogo ao `../teleconsulta`): guarda **docs/ + specs/ + .claude/rules + infrastructure + Makefile + governança**, **sem código de app**. O código vive em **`services/`** como **repos git SEPARADOS, gitignorados** pelo hub: `doctor-hub-api` e `doctor-hub-web`. O conhecimento do antigo `~/ptm/projects/doctor-hub/` (docs, decisions-log, fixtures, design, specs) foi **migrado** para o hub; o código antigo (`app/`, `server/`, `tests/`, `sync/`, `.env`) foi **descartado**. Baseline de scanning de segredo (`.gitleaks.toml` + `.pre-commit-config.yaml`) **herdado do teleconsulta** (branch protegido ajustado p/ `main`). **Doctor-Hub = 1º produto**; a plataforma nasce para servir produtos da empresa (Teleconsulta, Telediagnóstico, Dados…). | 2026-06-23 | Confirmado por Alessandro (2 perguntas: nome **ptm-platform**; services como **repos separados gitignorados**). Estende D-109 (que dizia "dois repositórios" soltos → agora vivem em `services/`). Continuidade: a memória do Claude estava na chave do path antigo — copiada para a chave do novo path. |
| **D-109** | **Início do build real — reset de código + stack confirmada (atualiza D-049).** Encerrado o ciclo de protótipo (Figma + tentativas em `app/`/`server/`); **descartar o código antigo e recomeçar do zero**, **preservando todo o conhecimento** (`docs/`, `specs/`, `design/`, decisions-log, fixtures). **Stack confirmada por Alessandro:** (1) Backend **.NET 10** (C#, fixo) · (2) Banco **PostgreSQL** (fixo) · (3) Acesso a dados **EF Core 10 + Dapper** (EF p/ CRUD+migrations; Dapper nas queries críticas de capacidade/relatório) — **atualiza D-049** (era Dapper-only) · (4) Frontend **React + Vite + TypeScript, mobile-first, PWA** (caminho a app de loja via Capacitor depois) · (5) Infra **GCP** (infra da Portal) — recomendação **Cloud Run + Cloud SQL + Secret Manager**, *a confirmar com o padrão de infra da Portal* · (6) **Dois repositórios** (api / web), git local primeiro, remoto depois. **Disciplina (não relaxar):** SDD+TDD (spec/teste antes), lotes pequenos, **seed do banco a partir da fixture canônica** (`docs/product/22-demo-fixtures.md`), CI como guardião de coerência (substitui "eu lembrar de checar"). | 2026-06-23 | Confirmado por Alessandro (3 perguntas: PWA React / EF Core+Dapper / dois repos). Reabre formalmente D-049. Segredos: zero no código, **Secret Manager**; o `.env` de dev antigo não migra. Auth (Identity vs IdP da Portal) = decisão própria futura, não inferir. |
| **D-108** | **Consolidação de telas duplicadas + 3º pilar de coerência (ciclo de vida de tela).** Homologando, o Alessandro pegou **duas Escalas paralelas**: v1 (`2:2` Ativo, polida) e v2 (`496:5971` Lista PROVISÓRIO + `499:5991` Criar) — eu criei a v2 e **não apaguei a v1**, e os cliques se dividiram (ficha→v1, menu lateral→v2). **Decisões:** (1) **Escala oficial = v1** (uma escala por médico) → **apagada a v2** (496/499); cards de médico repontados (Henrique→`2:2`, demais→`176:*` vazio). (2) **Só o Henrique tem escala** → revertido o split **3/3→1/5** (Juliana/Rafael voltam a "sem escala"; "tem escala" ↔ tem conteúdo real). (3) **Removido o "Laboratório de estados (QA/demo)"** (`552`) e as **10 telas de erro/borda** que só ele indexava (órfãs → apagadas: Login erro, CPF inválido, carregando, +esp/+exame erro, reativar confirmações, erro ao salvar, Reativar bloqueado). | 2026-06-22 | Confirmado por Alessandro (3 perguntas: v1 / só Henrique / remover QA). **Pilar novo (`docs/product/24-registro-telas.md`):** 1 tela canônica por intenção; **superou → apaga a antiga + repointa**; 2 detectores (inventário duplicatas/PROVISÓRIO/órfãs + consistência de clique) antes de "pronto" (no CLAUDE.md). Corrige o D-107 (que dizia split 3/3). **Estado pós: 75 telas, 75/75 alcançáveis, 0 vazamentos, 0 órfãs, 0 mortos, 0 duplicata de intenção.** Resíduo intencional: "Sobrepor →" do SES-AP vai direto ao Sobrepor coberto (exemplo). |
| **D-107** | **Fixture v2 (elenco real) + Contratação reorganizada por especialidade + modal de exportar.** (1) **Clientes reais** (substituem SP/RJ/MG/BA/PE): **SES-PI · SES-AM · SES-AL · IASEP-PA · SES-AP** (com CNPJ fictício). (2) **Perfis/usuários nomeados** (nome + CPF fictício): **Renata Troncoso** (Demandas Médicas), **Aldair Moura** (Regulação · SES-PI), **Eronildes Bastos** (Gestor · Teresina) — substitui "Millena Garbiati". (3) **Médico placeholder "Alessandro (dev)" → "Dr. Henrique Sampaio"**; split de escala passa de 1/5 → **3/3** (escalas preenchidas). (4) **Use cases nomeados** (Amapá coberto / Piauí em falta / Amazonas reservado / Alagoas reaberto / Alagoas cancelado / IASEP entregue). (5) **Relatório de contratação `511:6029` reorganizado POR ESPECIALIDADE** (headline = "contratar ~N médicos"; abaixo "para quem" = cliente · prioridade · faltam · pediu · prazo) — antes era lista plana e não dava pra ler o que/para quem contratar. (6) **Botão Exportar → modal de resumo `641:6188`** (~5 médicos, breakdown por especialidade, prioridade, prazo mais curto, formato). | 2026-06-22 | Confirmado por Alessandro ("vamos usar exemplos mais reais… nomes fictícios, documentos… nome para cada use case… o sistema precisa de mais personalidade" + "Sim pode propagar"). Fixture v2 = `docs/product/22-demo-fixtures.md` (fonte única). **Pendente de propagação** (task #20): listas de Inbox/multi-cliente/GG/GR ainda têm clientes ad-hoc (Prefeitura do RJ, Campinas, Joinville, Maranhão, Endocrinologia) a colapsar nos 5 canônicos. Próximos: Sobrepor com/sem capacidade (#22), status reservar/voltar/cancelar (#23), avatares+escalas dos médicos (#24). **✅ Sessão offline 2026-06-22 — todos concluídos:** (#20) clientes reconciliados nas listas (5 canônicos, 0 resíduos); (#22) Sobrepor COM capacidade `651:6207` (SES-AP coberto, verde) + SEM `517` (SES-PI em falta); (#23) tela **Estados e transições** `654:6226` (4 estados D-104 com ações permitidas), alcançável da tela de status; (#24) médicos renomeados (Dr. Henrique Sampaio, Dra. Fernanda Alves), **avatares coloridos por médico**, CRMs coerentes (CRM-PI/AM/AL/PA/AP), **escala split 3/3** (Henrique·Juliana·Rafael com escala) em Médicos **e** Escala. **Especialidades alinhadas ao construído** (Juliana=Cardiologia, Paula=Pediatria) p/ evitar inconsistência ficha×card. **Pós-tudo: 87 telas, 87/87 alcançáveis, 0 vazamentos, 0 órfãs, 0 mortos.** Gap menor conhecido (futuro): Juliana/Rafael mostram "tem escala" mas só Henrique tem a GRADE de escala montada (`2:2`); aprofundar o conteúdo de escala por médico fica para depois. |
| **D-106** | **Isolamento de persona na navegação + LINTER de navegação como gate (responde "como garantir o fluxo correto").** **Regra dura:** troca de persona acontece **só pelo avatar → "Entrar como" (Seletor)** — **nenhum botão no meio de uma tela** pode levar de uma persona (Demandas / Regulação / Gestor) para outra. Um clique dentro de Demandas **nunca** cai na tela de outra persona. **Gate automático** (rodar ANTES de dizer "pronto", junto da fixture de dados D-fixtures): um **linter de navegação** sobre o grafo de NAVIGATE do Figma que checa 4 invariantes — (1) **isolamento de persona** (0 saltos cross-persona, exceto avatar→Seletor); (2) **alcançabilidade** (toda tela acessível do Login via BFS); (3) **0 clicks mortos** (todo destino existe); (4) **0 órfãos**. Script + contrato em `docs/product/23-navegacao-contrato.md`. | 2026-06-22 | Confirmado por Alessandro (pegou o furo: "cliquei uma opção dentro de Demandas, caí no Regulação… como garanto que a apresentação represente o fluxo correto?"). **Furos corrigidos (13):** Sobrepor `517:6093` e Reservado `518:6109` botão **Provisionar** apontavam p/ a tela do Gestor `522:6125` → repontados p/ **Status da demanda `495:5955`** (Demandas, mostra Entregue); link "(Gestor) assumir/agendar →" no Status `495` (teleporte) → **removido** (handoff só via Seletor); ícone **profile** nas 5 telas de GG/GR apontava p/ "Conta · Perfil" `57:2` (que tem o **menu de Demandas**) → **removido**. **Pós-fix: 84/84 telas alcançáveis, 0 vazamentos, 0 clicks mortos, 0 órfãos.** Estende a regra de coerência do CLAUDE.md (dados via fixture → agora **navegação via linter**). |
| **D-105** | **Remanejamento — MANTER como etapa (≠ Captar); é realocar capacidade OCIOSA; ENTREGA FUTURA.** Corrige o conceito: remanejamento **não é contratar** (Captar/D-102). É **realocar slots que foram solicitados → reservados → disponibilizados → assumidos, mas que NÃO viraram agendamento** (ociosos), **antes de expirarem (~48h)**, para **outro cliente** que precisa daquela especialidade. **NÃO reduz o que foi disponibilizado** ao cliente original (ex.: cliente A recebeu 1000; 50 ociosos dele são realocados para o cliente B; **A continua com 1000**). Objetivo: **não desperdiçar capacidade já paga** (médico pago, slot assumido sem agendamento). Visão necessária: por cliente/especialidade — **slots ociosos + janela de vencimento (48h) + realocar para**. | 2026-06-21 | Confirmado por Alessandro. **É ENTREGA FUTURA** ("bem mais para frente; primeiro o básico") — manter como **etapa/placeholder** no fluxo, sem detalhar agora. **Impacto:** recriar a tela Remanejamento (foi removida na reconciliação) como **placeholder marcado "entrega futura"** com o conceito correto (capacidade ociosa a vencer), reachable mas não aprofundada. Distinto de D-047 (versão antiga) e de D-102 (Captar/contratação). |
| **D-104** | **Status oficial da solicitação — 4 estados + sub-estados + máquina de transição.** **4 estados:** **ABERTO · RESERVADO · ENTREGUE · CANCELADO.** **Sub-estados:** **Reservado** → {atendendo parcial \| atendendo total}; **Entregue** → {atendido parcial \| atendido total}. **Regras:** (1) **Aberto** = ainda não sobreposto (não dá pra saber cobertura). (2) **Cancelado** = não importa mais. (3) **Reservado e Entregue NUNCA vão direto para Cancelado** — para cancelar, **volta Reservado → Aberto** e só então Cancela. (4) **Solicitação não atendida no prazo → Cancelado** (motivo **"data expirada"**); se estiver Reservado, **volta a Aberto** antes de cancelar. (5) **Excluir = soft delete, só se ABERTO** — Regulação **e** Demandas podem; **excluído ≠ cancelado** (some do fluxo, não vira Cancelado). | 2026-06-21 | Confirmado por Alessandro. **Substitui o vocabulário ad-hoc** que eu vinha usando (Nova/Recebida/Em análise → **Aberto**; Draft/Em sobreposição/Provisionada → **Reservado**/**Entregue**; Coberta/Parcial → sub-estados). **Impacto Figma:** padronizar os badges de status em Início, Inbox, Minhas solicitações, Multi-cliente para os 4 estados + sub-estados; refletir as transições (botão "Voltar para Aberto" antes de Cancelar; Excluir só em Aberto). |
| **D-103** | **Solicitação: ad-hoc, MÚLTIPLAS por cliente, com "A PARTIR DE" (data de início); substitui o e-mail.** Cada cliente pode solicitar **quantas vezes quiser** — para o **mesmo mês** ou para o **próximo** (ex.: faltando ~10 dias pro fim do mês, já pede o próximo; às vezes de 15 em 15 dias; às vezes uma vez só). Hoje isso é feito por **e-mail** — o Doctor-Hub passa a **registrar** essas solicitações. O **Regulação** entra e solicita informando a **data de início** ("quero **X consultas a partir do dia 15 do mês que vem**") + itens (especialidade × qtd). A **Demandas Médicas**, assim que recebe, **tenta provisionar** (sobrepor→reservar→captar→provisionar→cliente recebe), e esse ciclo pode **repetir** várias vezes por cliente. | 2026-06-21 | Confirmado por Alessandro. **Impacto Figma:** **Nova solicitação** (Regulação `531:6141`) ganha campo **"A PARTIR DE"** (data de início) no lugar/além de "prazo até"; subtítulo deixa claro que pode solicitar várias vezes. Múltiplas solicitações por cliente já aparecem na Inbox/Minhas solicitações. **✅ Período (2026-06-21):** é uma **JANELA editável — "A PARTIR DE" + "ATÉ"**, com **default = até o final do mês**. Aplicado: Nova solicitação ganhou os 2 campos (range); colunas "PRAZO"→"PERÍODO" com ranges na Inbox/Minhas/Sobrepor. |
| **D-102** | **"Captar novos médicos" = ação OPERACIONAL, só no modo RESERVADO; gera Relatório de contratação CONSOLIDADO (por cliente, com prazos e prioridade).** A captação **não é por sobreposição** — a operadora reserva cliente por cliente (A provisionou, B provisionou, C faltou, D faltou em outra especialidade — todos em **Reservado**), e **depois** olha a OPERAÇÃO e capta em cima do que está reservado. Logo, **"Captar novos médicos" só fica disponível quando há reservas** (não no Sobrepor pré-reserva). Gera um **relatório ÚNICO consolidado** que mostra, **por cliente**, o quanto falta (especialidade + quantidade), com **prazos/datas**, um **total**, e **insights de prioridade — quem solicitou primeiro tem prioridade** (FIFO por data de solicitação). | 2026-06-21 | Confirmado por Alessandro. **Impacto Figma:** (1) **remover "Captar novos médicos" do Sobrepor** `517:6093`; (2) entrada da captação fica nas telas operacionais (contador "Sem capacidade" da Home + visão Multi-cliente); (3) **redesenhar o Relatório de contratação** `511:6029` como **consolidado**: colunas CLIENTE · ESPECIALIDADE · FALTAM · PRAZO · SOLICITADO EM · PRIORIDADE (ordem de solicitação) + total + insight de prazo. |
| **D-101** | **Sobrepor v2 — colunas com parâmetros globais + "RESERVADO" (nome oficial do draft).** A tabela do Sobrepor (por especialidade) mostra, para o cliente atual: **SOLICITADO** (o que o cliente pediu) · **DISPONIBILIZADO** (editável — quanto a Demandas dá) · **RETORNO** (editável/manual — D-100); e como **contexto/insight global**: **CAPACIDADE TOTAL** (nossa capacidade total daquela especialidade) · **TOTAL SOLICITADO** (soma de TODOS os clientes naquela especialidade em aberto). Assim a operadora vê quanto ainda precisa alocar — comparando a demanda total com a nossa capacidade. **"RESERVADO" = nome oficial do "draft".** Ao sobrepor e deixar em **Reservado**, ele **reserva da CAPACIDADE TOTAL** e **diminui do TOTAL SOLICITADO** (a demanda em aberto cai conforme é atendida). Ex. (Cardiologia): solicitado 1000 · disponibilizado 1000 · retorno 200 · capacidade total 7000 · total solicitado 13000. | 2026-06-21 | Confirmado por Alessandro. **Impacto Figma:** rebuild da tabela do Sobrepor (`517:6093`) com as 5 colunas; **renomear "Draft" → "Reservado"** em todo o fluxo (Home contador "Drafts em aberto"→"Reservados"; Inbox/status; Multi-cliente; botão "Salvar draft"→"Reservar"; tela Draft `518:6109`→"Reservado"). Mecânica de reserva (reserva capacidade total / abate total solicitado) é dinâmica — no protótipo, demonstrada por números + nota. |
| **D-100** | **Sobrepor: SOLICITADO (novos) + RETORNO (manual) = TOTAL A DISPONIBILIZAR; a Demandas define o retorno manualmente.** A solicitação do cliente é de atendimentos **novos** (ex.: 1000 cardios novos). Além disso há **retornos** — pacientes que já passaram naquela especialidade e precisam voltar (ex.: 200). O **retorno NÃO está no sistema hoje** (é controlado em **planilha**), por isso a **Demandas Médicas preenche a quantidade de retorno MANUALMENTE** por especialidade. **Total a disponibilizar = solicitado (novos) + retorno** (ex.: 1000 + 200 = 1200). O sistema **dá insights** do total por especialidade (NOSSA capacidade calculada das escalas + solicitação em aberto), mas **quem define o retorno é a operadora**. | 2026-06-21 | Confirmado por Alessandro. **Conecta com D-005** (estoque misto = base calculada + ajuste manual "retornos/extras") e **D-011** (retorno → último doutor preferencial, lá no agendamento). **Impacto Figma (Sobrepor `517:6093`):** tabela por especialidade ganha colunas **SOLICITADO (novos) · RETORNO (input manual) · TOTAL A DISPONIBILIZAR (=soma) · NOSSA CAPACIDADE (calc.) · COBERTURA**; o RETORNO é campo editável. Specs do projeto a alinhar: `specs/disponibilizacao/ui.md`, `specs/modelo-oferta-demanda/spec.md`. |
| **D-099** | **Navegação organizada POR PERSONA/JORNADA.** **Login → Seletor de perfil ("Entrar como…") → 3 jornadas**, cada uma com **sidebar/menu próprio** (a persona vê só o seu menu): **(1) Demandas Médicas** (Médicos · Escala · Pendências — workflow Sobrepor/Draft/Provisionar/Contratação/Multi-cliente/Remanejamento); **(2) Regulação** (cliente — Minhas solicitações → Nova solicitação · De acordo); **(3) Gestor** (unidade — Agendamentos → Assumir/Agendar). | 2026-06-19 | Confirmado por Alessandro ("um para cada jornada… depois do login… só o seu próprio menu"). **Figma:** Seletor `529:6141`; GG home `530:6141` + Nova `531:6141` + De acordo `531:6251`; GR home `532:6141` + Assumir/Agendar `522:6125`. Sidebars por persona feitas por **detach + reconfig** (não dava pra trocar itens via componente). **86 telas, 100% nav.** |
| **D-098** | **Escopo (b): o AGENDAMENTO acontece no Doctor-Hub (Gestor assume) + INTEGRAÇÃO de pacientes/doutores com a Teleconsulta.** Após o "de acordo" do Regulação, o **Gestor assume os slots** montando o agendamento (**doutor + paciente + horário + local**). A **Teleconsulta recebe o agendamento completo**. Para montar o agendamento, a **Teleconsulta DISPONIBILIZA ao Doctor-Hub**: (1) **lista de pacientes, por cliente**; (2) **lista de doutores, indicando qual foi o último doutor que atendeu aquele paciente naquela especialidade** (se houver) = preferencial. **Precisa de uma boa integração de pacientes e doutores com a Teleconsulta.** | 2026-06-19 | Confirmado por Alessandro. **Amplia a Entrega 1:** entra a tela **"Assumir / Agendar"** (papel **Gestor**). O "último doutor por paciente×especialidade" casa com **D-011** (preferencial). **⚠️ LGPD — PACIENTE entra no sistema agora** (lista de pacientes por cliente é dado sensível): tratar base legal/retenção/escopo/acesso (RBAC) — não inferir. **Integração:** PULL **read-only** de **pacientes + doutores** da TC (estende D-069, que era médicos RO; paciente é mais sensível). PUSH do **agendamento** para a TC. **Guarda-rail Figma:** revisitar a política "não expor sync/TC" — a integração agora é feature; representar as listas SEM expor o mecanismo de sync. Abertos em `03-open-questions.md`. |
| **D-097** | **Downstream pós-Provisionado: aceite do Regulação → Gestor assume → AGENDAMENTO → Teleconsulta.** (1) Após **Provisionado** (cliente notificado), o **Regulação dá "de acordo"** (aceite formal da disponibilização). (2) **Somente após o aceite**, a disponibilização fica disponível para o **Gestor** **assumir os slots** — definindo **doutor + paciente + horário + local** — o que se torna um **AGENDAMENTO**. (3) O agendamento **sincroniza com a Teleconsulta**. | 2026-06-19 | Confirmado por Alessandro. **Completa o pipeline** (Oferta→Demanda→Alocação→Remanejamento→**Agendamento**→Teleconsulta). **Reconcilia papéis (resolve `02-roles.md`):** **Regulação = o demandante** (≈ "Gestor Solicitante" `solicitante`); **Gestor** (`gestor`) = quem assume/agenda (papel já existente, D-009/D-011). **Paciente (refina D-093 #5):** NÃO entra na DEMANDA (agregada) — entra só no **AGENDAMENTO**, selecionado pelo Gestor ao assumir (= modelo antigo). **Status enum ganha:** `Provisionado` → `De acordo (aceite do cliente)` → `Assumido/Agendado`. **Guarda-rail Figma:** NÃO expor o sync/Teleconsulta na tela (política). **A DECIDIR (escopo):** as telas de aceite/assumir/agendamento entram na Entrega 1 do Doctor-Hub, ou são responsabilidade da Teleconsulta (Doctor-Hub só entrega até Provisionado/aceite)? |
| **D-096** | **Notificação ao cliente = na transição DRAFT → PROVISIONADO.** O cliente **só recebe a notificação** quando Demandas Médicas passa a disponibilização do status **draft** para **provisionado** (novo status). Ou seja: **draft** = capacidade reservada, **cliente não vê**; **PROVISIONAR** (a ação antes chamada "Enviar") = mover draft → provisionado, e **é isso que dispara a notificação** ao cliente. | 2026-06-19 | Confirmado por Alessandro. **Status enum ganha `Provisionado`** (após Draft). Mapeia no pipeline: **Provisionar ≈ emitir**. **Impacto Figma:** renomear ação "Enviar" → **"Provisionar"** no Sobrepor `517:6093` e Draft `518:6109`; adicionar status **Provisionado**. **Ainda aberto:** o que o cliente FAZ ao ser notificado (só vê / assume / agenda?) — decidir com Alessandro (sessão ao vivo). |
| **D-095** | **Refinamentos do workflow de demanda (decide as 3 perguntas de D-094).** (1) **Disputa de capacidade = DEMANDAS MÉDICAS DECIDE; o sistema só dá INSIGHTS** (ex.: quem precisa mais por **prazo**; quanto ainda falta contratar). No **draft** vê-se **quanto ainda precisa contratar** → botão **"Captar novos médicos"** → gera um **relatório entregue a uma EMPRESA RECRUTADORA externa**, que contrata os doutores no mercado nos próximos dias para cobrir a necessidade. (Refina o "Relatório de contratação" `511:6029`: destino = empresa que contrata.) (2) **ENVIAR** → dispara **notificação ao Regulação do cliente (talvez também aos Gestores Regionais)**; **no mínimo uma notificação DENTRO do sistema** (canal e-mail/WhatsApp = a definir). (3) **DRAFT:** pode ser **DESCARTADO → libera a capacidade reservada**; **NÃO expira**. **HOME:** deve ter um **relatório de PENDÊNCIAS** (quantos drafts, solicitações, etc.) para Demandas Médicas enxergar o que está pendente. | 2026-06-19 | Confirmado por Alessandro. **Revive uma HOME leve de pendências** (≠ cockpit deferido em D-087 — é a lista de trabalho da Demandas: drafts/solicitações pendentes). **Impacto Figma/app:** insights no Sobrepor/draft (urgência por prazo, gap a contratar); botão "Captar novos médicos" → relatório p/ recrutadora; "Enviar" gera notificação in-app ao cliente; draft com ação "Descartar" (libera reserva); **nova Home de pendências**. Abertos: canal da notificação; Gestores Regionais também notificados?; o que o cliente faz ao ser notificado (assume/agenda?). |
| **D-094** | **Workflow da DEMANDA: solicitação ad-hoc → SOBREPOR → DRAFT (reserva) → enviar, com travas de imutabilidade.** (1) **Solicitação ad-hoc:** o cliente (via **Regulação**) pode pedir **a qualquer momento** — formato "até `<prazo>`, quero X de cada especialidade" (prazo + lista especialidade×qtd; unidade = atendimentos/Teleconsulta por D-093). (2) **Notificação:** **Demandas Médicas** (operador PTM, papel `demandas`) recebe **notificação de nova solicitação**. (3) **SOBREPOR** (nova funcionalidade): Demandas **sobrepõe a nossa capacidade médica às solicitações do cliente** para simular se conseguimos atender (= o cruzamento demanda×capacidade, agora nomeado "Sobrepor"); produz uma **disponibilização**. (4) **DRAFT:** pode **salvar a disponibilização em modo draft** (sem enviar ao cliente); **o draft RESERVA a nossa capacidade de atendimento**. (5) **Multi-cliente:** Demandas faz para vários clientes; o sistema **ajuda a ver o total solicitado e quanto cada cliente pede**. (6) **TRAVAS:** (a) depois que o **cliente envia a solicitação, ele NÃO pode alterar nem excluir**; (b) a partir do momento em que Demandas **cria a sobreposição (mesmo em draft)** — equivalente a **"aprovar a solicitação"** — o registro da solicitação fica **IMUTÁVEL; só Demandas Médicas** pode alterá-lo (racional: o draft já reserva capacidade). | 2026-06-19 | Confirmado por Alessandro (raciocínio do fluxo). **Glossário novo:** Solicitação · **Sobrepor/Sobreposição** · Disponibilização (draft/enviada) · **Reserva** (o draft reserva). Mapeia no pipeline (Disponibilização simular/reservar/emitir): **draft ≈ reserva**, **enviar ≈ emitir**. Papéis: **Regulação** (cliente, solicita) + **Demandas Médicas** (`demandas`, sobrepõe/aprova/envia). **Impacto Figma/app:** telas de **inbox de solicitações (notificação)**, **Sobrepor**, **draft + reserva**, **visão agregada multi-cliente**, e os **locks** (cliente read-only pós-envio; solicitação imutável pós-sobreposição). Abertos em `03-open-questions.md` §Demanda-workflow. |
| **D-093** | **Modelo da DEMANDA confirmado (responde os 6 bloqueadores de D-092).** (1) **Unidade = ATENDIMENTOS**, **só TELECONSULTA por ora** (demanda de Telediagnóstico depois). (2) **Entrada por `health_center`** — `health_center` **≡ cliente** (pode ser um estado ou não); o perfil **GESTOR GERAL** acessa a tela e digita a necessidade. (3) **Periodicidade:** geralmente **mensal**, podendo ser **semanal**. (4) **"Cobrir" = ter capacidade de DISPONIBILIZAR**; sem capacidade → gerar **RELATÓRIO DE CONTRATAÇÃO** (informações p/ contratar mais doutores) — não é (só) remanejamento. (5) **Paciente NÃO entra** — demanda **agregada** (LGPD-friendly). (6) **Origem = DIGITADO** pelo Regulação (login + credencial + perfil); **não** é sync externo. | 2026-06-19 | Confirmado por Alessandro (respondeu os 6 bloqueadores). **Impacto Figma (demanda passa de PROVISÓRIO→confirmado nesses pontos):** reenquadrar "por estado" → **"por health center / cliente"**; solicitante = **Regulação**; unidade = **atendimentos (Teleconsulta)**; **construir tela "Relatório de contratação"** (saída da falta); demanda sem paciente. **Novo papel logável: GESTOR GERAL** (demandante) → atualizar `docs/discovery/02-roles.md`. **Ainda aberto:** precedência remanejamento × contratação; faixa de periodicidade mensal/semanal na UI. |
| **D-092** | **Entrega 1 passa a INCLUIR o fluxo de DEMANDA por estado/especialidade** (amplia D-087, que tinha mandado o lado-demanda p/ futuro). O Alessandro quer o fluxo "demandas solicitadas pelos estados" dentro da 1ª entrega. | 2026-06-18 (escopo; regras PENDENTES) | Confirmado por Alessandro ("refinar a entrega 1, que inclui a parte do fluxo de demandas solicitadas pelos estados"). **⚠️ As REGRAS da demanda NÃO estão definidas** (D-083 só dizia "vem por estado×especialidade"). Logo: o protótipo do fluxo de demanda será **PROVISÓRIO** (proveniência real/demo, cada regra = pergunta aberta em `03-open-questions.md`) — **não inferir** (dinheiro público + estados). Trabalho no loop overnight `docs/product/19-overnight-loop-2026-06-18.md`. |
| **D-091** | **Modelo de ESCALA evoluído: múltiplas escalas por médico + FIXA vs FLEXÍVEL (supera D-063 "1 ativa").** (1) **Múltiplas escalas por médico** — uma **por especialidade (Teleconsulta) ou por produto/modalidade (Telediagnóstico)**; não é mais "só 1 ativa por médico". (2) **Escala FIXA** = a de hoje, mas **sem data de fim** (vigência aberta). (3) **Escala FLEXÍVEL** = tem **início + fim**; **substitui o conceito de "horas adicionais"** (que hoje é de 1 dia); **não pode conflitar com a escala fixa**. (4) Os dados de escala são **imutáveis para histórico** — nunca se exclui escala/especialidade/produto que teve uso (laudo/atendimento); só **inativar + arquivar** (ex.: "qual era a escala dele em janeiro do ano passado"). | 2026-06-18 (PARCIAL — detalhes a confirmar) | Confirmado por Alessandro (resposta às perguntas de validação). **Supera D-063** (escala mensal, 1 ativa por médico) e **D-065/D-072** (horas adicionais → vira escala flexível). **Reabre desenho do módulo Escala.** Ambiguidades a confirmar em `03-open-questions.md` §Escala-v2 (escala é 1 por especialidade OU cobre ambos os produtos?; demais regras da flex além de "não conflita com a fixa"?; faturamento por escala). |
| **D-090** | **Regras de faturamento na ficha (respostas de validação).** (a) **Validação ao adicionar/salvar** especialidade/exame: **valor > 0 obrigatório** + **não pode duplicar** a especialidade/exame que o médico já tem. (b) **RBAC Fase 1:** o **mesmo perfil (Demandas Médicas)** que monta escala também edita o faturamento (RBAC fino = Fase 2). (c) **Escala editável + cancelável** (refina D-073: além de criar/arquivar, pode-se editar uma escala ativa e cancelá-la com confirmação). (d) **Não-exclusão reforçada (refina D-089):** especialidade/exame que já teve escala **com uso** (laudo/atendimento) **nunca** é excluído — só inativado/arquivado (histórico). | 2026-06-18 | Confirmado por Alessandro. Impacto Figma: validação nos modais de faturamento (valor vazio/≤0/duplicado → erro); fluxo de editar/cancelar escala. |
| **D-089** | **Especialidade/exame de faturamento: INATIVAR/ALTERAR é o padrão; EXCLUIR só se nunca foi usada (integridade/auditoria).** Não se **exclui** uma especialidade (Teleconsulta) nem um exame/modalidade (Telediagnóstico) que já tenha gerado uso — o correto é **Inativar** (deixa de faturar, **mantém o histórico**) ou **Alterar** (editar valores/modo). **Excluir** (apagar de vez) **só é permitido se a especialidade/exame NUNCA teve escala vinculada** (nunca foi usada). Isso refina D-088 (o "× remover" vira **Alterar + Inativar**, com **Excluir** só no caso never-used). | 2026-06-18 | Confirmado por Alessandro ("o certo é não poder excluir especialidade ou modelo, mas sim inativar ou alterar; excluir só se nunca teve uma escala para aquela especialidade vinculada"). **Impacto Figma:** ações por linha = **Alterar** + (**Inativar** se tem escala \| **Excluir** se nunca teve). Demo teleconsulta: Cardiologia (tem escala)→Inativar; Clínico Geral (sem escala)→Excluir. **A confirmar (open-questions):** p/ **Telediagnóstico** não existe "escala" — o critério de "nunca usada" é **nunca ter emitido laudo**? (provisório: mesmo padrão). Inativo de especialidade ≠ inativo do médico (D-084). **Refino UX (2026-06-18):** ações por linha = só **Editar** (✎) + **Excluir** (🗑, com ícones). O **Excluir VALIDA**: se tem escala/uso → modal "**Não é possível excluir** — será **inativada**, mantém histórico, **pode ser reativada**"; se nunca usada → confirma e exclui de vez. **Alterar/Editar** nunca muda a especialidade/exame em si (campo travado 🔒 — p/ trocar, exclui e adiciona). 6 use cases (editar esp/exame; excluir-com-uso→inativa; excluir-sem-uso→exclui) construídos e wired na editar canônica (17:2). |
| **D-088** | **Fluxo de criar/excluir linhas de faturamento na ficha (editar).** (1) **Adicionar = MODAL** (popup com formulário). Teleconsulta: modal "Adicionar especialidade" (especialidade + RQE + modo hora/consulta + valor + tempo). Telediagnóstico: modal "Adicionar exame/modalidade" (exame/modalidade + valor/laudo). [Cancelar]/[Adicionar]. (2) **Excluir = ícone × por linha → confirmação inline** ("Remover X? afeta o faturamento" · [Cancelar]/[Remover]) — padrão destrutivo do app (D-070/D-072). | 2026-06-18 | Confirmado por Alessandro ("criar o fluxo de criação e exclusão tanto de especialidade quanto de exame/modalidade" + escolheu "Modal"). **Impacto Figma:** novos frames no fluxo da editar — modal add-especialidade, modal add-exame, confirm remover-especialidade, confirm remover-exame; × nas linhas. **Regras a confirmar** (open-questions): pode remover a última especialidade? validação de duplicada? campos obrigatórios no add? |
| **D-087** | **Entrega 1 ENXUTA = Médicos + Escala. "Visão geral / Cockpit" e "Relatório" vão para ENTREGA FUTURA.** O Alessandro decidiu reduzir o escopo da 1ª entrega: fica só **Gestão de Médicos** (cadastro-dono + faturamento por produto) e **Escala**. **Movidos para entrega futura** (telas deletadas do protótipo, mas **intenção preservada** nos docs): (a) **Visão geral / Cockpit** — Capacidade de entrega, Projeção de capacidade, pool de especialidade, saneamento, Home com manchete/KPIs (D-082/D-083/D-084); (b) **Relatório** — Relatório de escalas, Capacidade/heatmap + drills, Cobertura por período Hoje/Amanhã/Semana/Mês (D-079/D-080). | 2026-06-18 | Confirmado por Alessandro ("sobre a tela de relatório, remover e mover p/ entrega futura; guardar só a intenção; vamos alterar um pouco a entrega 1" + escolheu "Mover Cockpit + Relatório p/ futuro"). **Impacto Figma `snTNGRUJO2GwoKpXTHCBjf`:** deletadas 15 telas (8 Relatório + Home + 6 do cockpit); **sidebar reduzida a Médicos · Escala** (itens "Visão geral" e "Relatório" removidos); **landing pós-Login = Médicos · Localizar**. Intenção detalhada em `docs/product/18-entrega1-enxuta-e-futuro.md` + D-079..D-084 + docs 15/16/17. **NÃO descarta o trabalho** — é repriorização de fase. |
| **D-086** | **Faturamento SEPARADO POR PRODUTO — Teleconsulta vs Telediagnóstico (refina D-085).** (1) São **dois produtos** com modelos de cobrança distintos; o faturamento é separado por produto. (2) **Teleconsulta:** cobrança **por hora OU por consulta**, e o **valor (da hora ou da consulta) é vinculado à ESPECIALIDADE**. Precisa de **escala de Clínico Geral** (Clínico Geral volta a ser especialidade escalável p/ teleconsulta — revisita D-067). O "valor por atendimento" de D-085 = o modo **"por consulta"** do Teleconsulta. (3) **Telediagnóstico:** modelo **por LAUDO de exame** (unidade ≠ consulta/hora). | 2026-06-18 (parcial — detalhes pendentes) | Confirmado por Alessandro ("temos teleconsulta e telediagnóstico, separar faturamento; teleconsulta por hora ou por consulta, valor vinculado à especialidade; teleconsulta precisa de escala de clínico geral; telediagnóstico é por laudo de exames"). Está escrevendo **aos poucos** → detalhes em `03-open-questions.md` §Faturamento por produto (modo hora/consulta por especialidade?; valor por laudo por médico/exame/modalidade?; médico serve os dois produtos?; tempo de atendimento só p/ por-consulta?). **Impacto:** redesenhar a seção de valores da ficha (de "valor por atendimento por especialidade" p/ **bloco Teleconsulta** [especialidade · modo · valor · tempo] + **bloco Telediagnóstico** [por laudo]). Atualiza D-085/D-067/D-072. Depois: **homologar** e seguir p/ o próximo. |
| **D-085** | **Modelo de valor = POR ATENDIMENTO + TEMPO DE ATENDIMENTO default por médico (substitui valor/hora).** (1) O médico cobra **por atendimento**, não por hora (ex.: Alessandro · Cardiologia · **R$ 40/atendimento**). (2) Cada médico tem um **tempo de atendimento default** (ex.: **20 min**) — é o que define a **capacidade**: slots = turnos × (60 ÷ tempo de atendimento) − intervalos; e o **faturamento previsto** = slots × valor por atendimento. (3) **A ficha do médico** passa a mostrar **"Valor por atendimento"** + **"Tempo de atendimento"** (saíram "Valor/h fixa" e "Valor/h adicional"). (4) **O valor da hora adicional NÃO fica na ficha** — só aparece **na tela de Escala, no momento de lançar horas adicionais** (refina D-065/D-053). (5) **Completude p/ faturamento** passa a exigir **valor por atendimento** (no lugar de valor/h fixa). | 2026-06-18 | Confirmado por Alessandro ("não é por hora, é por atendimento; tempo de atendimento default por doutor — Alessandro atende cardio em 20 min e cobra R$ 40/atendimento; tirar valor/h adicional da ficha, só na escala ao lançar horas adicionais"). **Aplicado Figma `snTNGRUJO2GwoKpXTHCBjf`:** fichas (ver/editar/incompleto/inativo × médicos) + históricos + saneamento. **CASCATA PENDENTE (não recalculada — ver open-questions):** os números de **slots/capacidade** hoje usam duração antiga (escala mostra "15 min"/"64 slots"; cockpit 2.249). Precisam **derivar do tempo de atendimento** — e o tempo/valor é **por médico × especialidade** (✅ confirmado por Alessandro 2026-06-18). **Modelagem:** tempo+valor vão para `doctor_specialties` (junto do RQE — D-064); a ficha mostra-os **na tabela de Especialidades** (Especialidade · RQE · Tempo · Valor/atend.), e os campos de nível-médico saem. Atualiza D-053/D-061/D-064/D-065/D-072. |
| **D-084** | **Tela "Capacidade de entrega" (refina D-083) — 3 definições do Alessandro.** (1) **Herói duplo:** "Médicos ativos" + "Capacidade de atendimento" lado a lado, com **navegação entre visões**. A capacidade é **fatiável por especialidade × dia da semana × duração da consulta** e exibida na **linguagem da tabela de disponibilidade** (atendimentos/hora) — ex.: "X consultas de 20 min de Cardiologia na terça". (2) **Saneamento da base entra no conceito:** médicos que não atendem mais são **inativados**; **médico inativo NÃO conta como pendência se não tem escala E não tem valor de pagamento** → "incompleto/Travada" e o **denominador da Taxa de ativação excluem inativos sem escala e sem valor**; precisa de **fluxo de inativar**. (3) **Gráficos INTERATIVOS** (não números estáticos): o usuário **monta a visão** por **janela temporal** (até fim do dia · semana · até fim do mês · custom); unidade (slots/semana **e** atendimentos/dia) **deriva do recorte**, não é fixa. | 2026-06-18 | Confirmado por Alessandro (3 respostas: "os dois lado a lado, navegar nas visões… capacidade de atender X consultas de 20 min de Cardiologia na terça"; "saneamento da base… inativar… inativo não conta como pendência se não tiver escala ou valor"; "gráficos interativos, montar a visão até fim do dia/mês"). **Impacto Figma `snTNGRUJO2GwoKpXTHCBjf` — CONSTRUÍDO 2026-06-18** (ver doc `17` §7): `279:1916` Demanda×Oferta → **Capacidade de entrega** (seletor de janela; herói duplo Médicos ativos 612 + Capacidade 2.249; Ociosa/Travada em âmbar; callout = construtor de visão → grade real `237:1790`; `capacityTable` sem demanda/gap). `284:1934` → **Projeção de capacidade** (slots/sem caindo pelas vigências, 2.249→1.894). Home `28:2` manchete = história de ativação real. **Removidos** órfãos `283:1941`/`288:1970` (Suprir gap). Saneamento reusa Inativar (`active`; `92:2`/`55:2`/`111:2`). Nav 58/58 (100%). Flag "inativo" já existe (`doctors.active`) → resolvido em `03-open-questions.md`; resta só critério de SUGERIR inativação + efeito no denominador. |
| **D-083** | **A DEMANDA não é inventada — vem por ESTADO × ESPECIALIDADE (fases futuras). Enquanto o estado não envia, a demanda NÃO EXISTE.** Portanto o cockpit C-level mostra a **NOSSA CAPACIDADE DE ENTREGA / OFERTA** (o que conseguimos entregar), **não** uma "demanda × oferta" com demanda-demo nem um "gap" contra número fabricado. Quando a demanda real chegar (por estado/especialidade), aí sim se sobrepõe demanda × capacidade. **SUPERSEDES a abordagem demo de D-082-ish nas telas de cockpit** (números de demanda inventados saem). | 2026-06-18 | Confirmado por Alessandro ("pensar melhor demanda×oferta; demanda vem dos estados por especialidade; preciso ver nossa capacidade de entrega; demanda sem estado não existe — melhor só a visão da nossa capacidade, não demanda inventada"). **Impacto Figma `snTNGRUJO2GwoKpXTHCBjf`:** reformular `279:1916` (Demanda×Oferta → **Capacidade de entrega**); remover demanda-demo/gap/"preciso 100·faltam 36"/severidade-Contratar; MANTER oferta auditável (slots reais D-072), pool ocioso (`291:1952`), saneamento incompletos (`294:1970`). Forecast `284:1934` de "gap" idem (vira projeção da NOSSA capacidade, ou placeholder futuro). "Demanda por estado" = placeholder marcado "próximas fases". Resolve open-questions §Cockpit (origem da demanda). |
| **D-082** | **KPIs das telas de Relatório agregadas (Semana/Mês) = métricas PRÓPRIAS do período** (não números de um dia). Definições: (a) **Médicos distintos no período** = nº de médicos com ≥1 turno no período (nº-demo coerente no protótipo; real = contagem distinta sobre as escalas); (b) **Especialidades cobertas** = especialidades com **≥1 slot** no período; (c) **Especialidades sem cobertura** = especialidades com **0 slots** no período; (d) **Slots previstos no período** = soma da grade (= grande total). Rótulo do card de slots muda por granularidade ("NO DIA / NA SEMANA / NO MÊS"). **Nas telas de DIA (Hoje/Amanhã), os KPIs permanecem diários.** | 2026-06-17 | Confirmado por Alessandro (escolheu "Métricas próprias do período"). Aplicado Figma `snTNGRUJO2GwoKpXTHCBjf`: Semana `258:1880` (57·5·0·1.509) e Mês `260:1898` (61·5·0·2.761); grades batem por linha e coluna. Pendência menor: "sem cobertura = 0" não deve usar a cor vermelha (0 é bom). Granularidade dia(11 esp) × semana/mês(5 esp na grade) a reconciliar quando vier dado real → `03-open-questions.md`. |
| **D-081** | **Sidebar ganha estado "Relatório" (variante `active=relatorio`).** O item "Relatório" do menu existia mas **não tinha como ficar destacado** — as 8 telas de Relatório marcavam "Escala" (tela ≠ item destacado). Criada a variante `active=relatorio` no componente Sidebar (`169:6`) seguindo o padrão de destaque (fill branco @12% + texto 100% + Semi Bold) e aplicada às 8 telas de Relatório, com as reações de navegação re-coladas por instância (o item Relatório nunca dá self-link: a home do relatório aponta para a Capacidade e vice-versa). | 2026-06-17 | Auditoria de maturidade do Figma. Alcançabilidade desde o Login mantida em 100% (52/52). Componente Sidebar agora tem 5 variantes (visao/medicos/escala/relatorio/none). |
| **D-080** | **Relatório evolui para "heatmap de capacidade" interativo + filtros + painel "Foto".** Após análise de UX (agente), a grade de presença vira **heatmap**: linha Especialidade→Médico × coluna **hora** (granularidade **1h** default, drill p/ 30min num recorte), **célula = nº de atendimentos previstos/hora** (**capacidade = slots da escala, D-072**: 60÷duração; somável por médico/especialidade/faixa) + **cor por intensidade**. **"Sem cobertura" preservado** como camada **vermelha** (canal separado da magnitude — o cliente "adorou"). **24h** com **madrugada colapsada mostrando o contador** (não some). **Barra de filtros** (especialidade/médico/janela horária/granularidade/dia) + **painel "Foto" reativo** que responde "quantos atendimentos na quinta 18/06, 08–09h, Cardiologia → 12" (soma do recorte, drill por clique). **Subtotais** por médico (coluna TOTAL) e por hora (linha TOTAL) + grande total. Aba **Capacidade** ↔ **Cobertura do dia** (grade D-079). | 2026-06-17 | Confirmado por Alessandro (2 escolhas: "capacidade = slots da escala/D-072"; "número + cor em 1h, drill p/ 30min") + requisitos: 24h, atendimentos/hora por médico, soma por filtro, tabela interativa filtrável. Figma frame "Relatório · Capacidade". Definição fina (feriados, turno noturno, capacidade por sala) = futuro/`03-open-questions.md`. |
| **D-079** | **Dashboard "Relatório de escalas" (planejamento, dia-a-dia).** Nova tela: grade **Especialidade → Médico × faixas de 30 min** (08:00–17:30), fiel à linguagem do relatório atual (`extras/escala.pdf`), mas **fonte = nossas escalas (planejamento)**, NÃO a operação. Cores: 🟢 médico escalado (plantão previsto) · 🔴 especialidade sem cobertura no horário · ⚪ fora da escala do médico. Cancelado/ausente/agenda-de-retorno (🟠/🟣 do PDF) **ficam de fora** (são operacional/realizado = integração futura). **Recorte de período** por seletor (Hoje/Amanhã/Semana que vem/Até fim do mês) + **navegação dia-a-dia** (‹ data ›). Indicadores do dia (planejamento): médicos escalados, especialidades com/sem cobertura, slots previstos. Acesso por item **"Relatório"** na sidebar (componente). | 2026-06-17 | Confirmado por Alessandro (3 escolhas: "planejamento, não operacional"; "dia-a-dia com navegação igual ao PDF"; referência `extras/escala.pdf`). Figma `snTNGRUJO2GwoKpXTHCBjf` frame "Relatório de escalas". Espelho operacional (com realizado/cancelado/retorno) = evolução futura, depende da integração da operação. |
| **D-078** | **Histórico de atendimentos por vigência (na ficha do médico).** Seção na ficha listando **cada escala/vigência** (início–fim) com: especialidade, dias·horário, **atendimentos PREVISTOS** na vigência (slots da escala, D-072), **realizados = placeholder "0 / em breve"** (integração futura) e status (ativa/arquivada) + total previsto nas vigências. | 2026-06-17 | Confirmado por Alessandro ("vigências + previstos; realizados em breve"). Coerente com D-072 (realizados reais = integração futura). Figma: card "Histórico de atendimentos · por vigência" na ficha. |
| **D-077** | **"Passar a limpo": novo arquivo Figma da Fase 1 (Médicos + Escala), homologado.** Criado arquivo limpo `snTNGRUJO2GwoKpXTHCBjf` (o antigo `NCMcYURZgrHH36f9DTk7di` ficou legado/confuso, **não é a entrega**). Leva só o necessário da Etapa 1: **Gestão de Médicos** (Localizar; Ficha ver/editar: dados+CPF-máscara+Nascimento≥18+valores+Especialidades/RQE+Editar/Salvar; Histórico LGPD; Escala-resumo+Gerenciar escala) e **Escala** (Localizar; Ativo; Criar +erro+sem-especialidade; Vazio; Arquivado/Histórico+Reativar; Confirmações; Modais Linha-do-tempo e Horas-adicionais). Tokens da marca recriados (coleção Tokens, vars 1:3..1:25). **Protótipo navegável numa única página** (limitação: prototype da Plugin API só navega entre frames da MESMA página → consolidado), sidebar Médicos↔Escala + handoffs ligados; início = Médicos Localizar. **Homologado contra `docs/design/fase1-acceptance-checklist.md`: todos os [Required] cobertos**; divergências D-075 aplicadas (Confirmar arquivamento NEUTRO; rótulo "Horas adicionais"; badge slots/semana azul-info). | 2026-06-17 (madrugada autônoma) | Confirmado por Alessandro ("criar novo projeto Figma, levar só o necessário da etapa 1 = gestão de médicos + escalas; passar a limpo; homologar; tocar até 6h"). Fonte da verdade = código (`MedicosPage.tsx`/`EscalaPage.tsx`). GAPS nice-to-have (não-Required): Localizar empty/overflow, Reativar-bloqueado, timeline-várias, Horas-vazio. Log: `docs/design/fase1-figma-passar-a-limpo.md`. |
| **D-076** | **1ª entrega ganha Configurações + Perfil (do usuário) + protótipo Figma navegável de TODOS os use-cases da Escala.** (a) **Perfil** (`/conta/perfil`): identidade do usuário (nome, e-mail, papel, organização, avatar) — ver/editar. (b) **Configurações** (`/conta/configuracoes`) com 4 seções: **Conta & Segurança** (alterar senha, sessões ativas/revogar), **Notificações** (toggles: escala criada/arquivada, pendências de cadastro, resumo semanal), **Preferências** (idioma pt-BR, tema claro/escuro, densidade), **Privacidade & LGPD** (baixar meus dados, histórico de acesso). Acesso via **menu do avatar** (popover) no rodapé da sidebar. (c) **Protótipo Figma** (M1) cobre todos os estados da Escala: Ativo, Localizar (+ filtros Com/Sem escala), Criar (+ erro de validação, + sem especialidade), Vazio, Arquivado/Histórico, Reativar-bloqueado, Confirmações, Modais (Linha do tempo simples e c/ várias; Horas adicionais vazio e preenchido) — todos clicáveis. | 2026-06-16 | Confirmado por Alessandro ("criar todos os use-cases… fluxo tão navegável que parece o sistema"; "tela de configurações e uma de perfil, ao lado do avatar"; "incluir na primeira entrega"). **⚠️ AMPLIA o escopo da 1ª entrega (D-052 = escala + cadastro-dono)** → refletir na estimativa/proposta (`docs/product/`). Código ainda não espelha (Figma-first). |
| **D-075** | **Redesenho da tela Escala (Figma-first, navegável) + rótulo "Horas adicionais".** Avaliação por agentes (UX/design/Figma) → correções aplicadas no Figma ANTES do código (executa o processo do D-074): (a) **1 CTA laranja por tela** ("Criar nova escala"); (b) badge "slots/semana" vira **azul-info** (verde só p/ status `ativa`/`tem escala`); (c) grade de métricas com **herói** ("Total previsto" em Montserrat navy) + secundárias; (d) **vermelho só p/ Excluir** (Arquivar = neutro, separado por divisória); (e) "Escala" aparece **1×** (removido topbar; **perfil no rodapé da sidebar**); (f) **Linha do tempo** (Gantt) e **Horas adicionais** viram **modais** acionados por botão (desafogam a tela; timeline só faz sentido com ≥2 escalas). **Rótulo de UI "Horas adicionais"** substitui "Disponibilidade adicional" (mesmo conceito D-053; código segue `additional_*`, D-057; glossário atualizado). | 2026-06-16 | Confirmado por Alessandro (sessão de redesign: "perfil no canto inferior esquerdo"; "Escala 2×?"; adicional/timeline "em um modal"; "criar nova escala grudado não está legal"; rótulo "horas adicionais"). Figma `NCMcYURZgrHH36f9DTk7di` página M1: frame "Escala · Doctor-Hub (v2)" (158:2) + modais (170:2, 171:2). **Pendente:** demais estados + protótipo navegável; depois espelhar no código. |
| **D-073** | **Modelo de continuidade da escala (nunca sobrepõe):** (a) **Início = momento da criação** — a escala "começa a partir da hora em que foi criada"; **não se cria escala retroativa** (start_date no passado → 400; o form fixa início = hoje). (b) **Ao ARQUIVAR, o fim efetivo vira a data do arquivamento** (a escala "parou" ali); ao **REATIVAR**, o fim volta ao fim previsto. Implementado como regra de exibição/cálculo: fim_efetivo = `archived_at` se arquivada, senão `end_date`. Assim as vigências **nunca se sobrepõem** (cada uma vai de start=criação até o arquivamento, e a próxima começa depois). (c) **1º dia proporcional:** se a escala é criada no meio do dia, conta-se só as horas restantes daquele dia (slots ≥ hora de criação), não o dia inteiro; dias seguintes, cheios. (Feriados e proporcional do dia de arquivamento = PROVISÓRIO/futuro.) | 2026-06-16 | Confirmado por Alessandro ("arquivar → data final = data de arquivado; reativar volta; não criar retroativa; inicia na hora criada; criar hoje o dia todo conta só as horas restantes"). |
| **D-072** | **Tela Escala funcional: timeline + métricas + arquivar seguro + reativar.** (a) **Timeline/Gantt** das escalas (ativa + arquivadas) num eixo de tempo. (b) **Métricas por escala:** dias de vigência, **atendimentos previstos** (slots na vigência — `atendimentosNaVigencia`, PROVISÓRIO: sem feriados) e **realizados = 0 fixo** (placeholder rotulado "integração em breve" — não é dado real). (c) **Arquivar seguro:** botão separado do "Criar nova escala" + **confirmação inline** (2 cliques, sem window.confirm) → evita arquivamento por engano. (d) **Reativar = "desfazer o arquivamento" SEGURO** (nunca excluir): `archived_at` (migration 011) + `POST /api/schedules/{id}/reactivate`. **Só a ÚLTIMA escala** (mais recente) **e SÓ quando NÃO há ativa** → reativa (sem ressuscitar antigas nem criar conflito); 409 com mensagem se violar. Botão "Reativar" no front aparece só na última arquivada + sem ativa; nas outras, nota explicativa. (e) **Métricas cristalinas por escala:** Início · Fim previsto · Encerrada em (archived_at) · Duração (dias) · Atendimentos/dia · /hora · Total previsto. Timeline com eixo de datas. | 2026-06-16 | Confirmado por Alessandro ("timeline do histórico; dias vigente; atendimentos previstos; realizados 0 com aviso futuro; separar criar/arquivar; reativar a última, nunca excluir"). Realizados reais = integração futura. |
| **D-071** | **Uplift de UX (boas práticas: simplicidade + cor SEMÂNTICA + acessível).** (a) **Sistema de cor semântico**: botões por função — primary/azul=confirmar (Salvar), secondary/neutro=Cancelar/Editar, **danger/vermelho SÓ para destrutivo** (Remover/Arquivar), sempre com rótulo textual (nunca só cor). (b) **CPF**: máscara 000.000.000-00 ao carregar + **validação de dígito verificador no front E no backend** (PUT → 400 se inválido). (c) **Date picker** no Nascimento: calendário (showPicker) + texto dd/mm/aaaa, **só passado + idade ≥ 18**. (d) **Lista de médicos ordenada por criação DESC** (respeita a TC): coluna `tc_created_at` (migration 010) importada na sync; `ORDER BY tc_created_at DESC NULLS LAST, name`. **Recomendações aceitas:** nome NÃO dividido em first/last (read-only da TC, divisão de nome PT é frágil); CRM/RQE sem máscara rígida (formato variável). | 2026-06-16 | Confirmado por Alessandro + pesquisa de boas práticas (data-table/dashboard UX, cor semântica). Pendente: popular `tc_created_at` exige re-upar o lease do Vault (acesso RO). Próximo: responsividade + insights do dashboard. |
| **D-070** | **Cadastro do médico: modo VER/EDITAR + máscara de CPF + LOG de auditoria (LGPD).** (1) Campos do cadastro ficam **read-only por padrão**; botão "Editar" libera + "Salvar"/"Cancelar" (o salvar envia TODOS os valores atuais, não apaga campo não-tocado). (2) **Máscara de CPF** 000.000.000-00. (3) **Auditoria** `doctor_changes` (migration 009): registra **quem, quando, campo, valor anterior → novo** em cada PUT do cadastro; `GET /api/doctors/{id}/changes` + painel "Histórico de alterações" na tela Médicos. Cobre os campos-dono (CPF, telefone, e-mail, nascimento, valores, especialidade principal). | 2026-06-16 | Confirmado por Alessandro ("máscara no CPF; clicar em editar p/ modificar; log de quem/o quê/campo/valor anterior/novo"). **Follow-up:** auditar também add/remove de especialidade e mudança de RQE (hoje só os campos-dono). |
| **D-069** | **SYNC de médicos VIA BANCO DE DADOS — PULL e PUSH (reverte a regra "só RO" do D-002/D-054).** O Alessandro vai criar uma **credencial dedicada com escrita em tabelas específicas** do core/auth da TC; a sync passa a ser **sempre via banco**, nos dois sentidos. **PULL** (TC→Doctor-Hub, RO): tool `sync/sync_doctors.py pull` traz id/nome/CRM/especialidade + **CPF, e-mail, nascimento, telefone** + semeia `doctor_specialties` (configurável por env, idempotente). **PUSH** (Doctor-Hub→TC, escrita): construído como **moldura SEGURA e INERTE** — só executa com `TC_WRITE_ENABLED=true` + credencial dedicada (`TC_WRITE_DSN`) + allowlist `tabela:colunas` + `--apply`; **nunca** DELETE/DROP/TRUNCATE/ALTER; todo UPDATE com WHERE por `external_id`; dry-run mostra diff; log de auditoria. **O MAPEAMENTO de push (campo→tabela:coluna) é confirmado pelo Alessandro antes de ligar — não inferido.** | 2026-06-16 | Confirmado por Alessandro ("sync sempre via banco, pull e push; credencial com escrita em algumas tabelas; ter cuidado"). **Risco alto** (escrita em prod de saúde/LGPD) → travas acima. Estrutura em `sync/` (tool + config.example.env + README). Atualiza D-002/D-054. |
| **D-068** | **DUAS telas separadas: "Médicos" (cadastro) e "Escala" (dedicada)** — supera a ficha única do D-067 (ainda concentrava ação demais). **Médicos** (`/medicos`): cadastro do doutor (dados, especialidades+RQE, valores) + escala em **LEITURA** (resumo da ativa + nota do histórico) + botão "Gerenciar escala →" que abre `/escala?doctor=<id>`. **Escala** (`/escala`): **busca própria** + aceita `?doctor=` vindo de Médicos; criar/arquivar escala (fluxo guiado) + **disponibilidade adicional** (só com escala ativa). Nav vira dois itens (Médicos · Escala); `/medicos-escala` redireciona p/ `/medicos`. Localizador de médico compartilhado entre as duas. | 2026-06-16 | Confirmado por Alessandro ("separar a tela: cadastro numa, escala em outra; na tela do doutor vejo a escala mas edito em tela dedicada"). Cada tela com propósito único (menos ação por tela). Atualiza D-067. |
| **D-067** | **Médicos & Escala = "Localizar → FICHA do médico" (workspace), NÃO wizard linear.** O wizard de 4 passos (D-066) foi **removido** como container global: confundia CRIAR com ATUALIZAR. Agora: busca → **ficha do médico** (seções de navegação livre, cabeçalho "Gerenciando — Dr. X"): Dados (cadastro pré-preenchido — `doctor_specialties` semeado da especialização da TC; sem label "(Teleconsulta)"; datas **dd/mm/aaaa** via `DateFieldBR`; **Clínico Geral fora do seletor de atribuição**, mantido no filtro de busca); Escala (ativa + histórico; botão "Criar nova escala" = **único fluxo guiado/wizard**, avisa que arquiva a anterior); Adicional = sub-seção **só com escala ativa**. **Wizard reservado ao ATO de criar escala.** | 2026-06-16 | Confirmado por Alessandro ("wizard p/ criar; update não é wizard; tela clara/fluida/interativa") + painel multi-agente de UX (4 lentes, nota 2,0/2,0 p/ a versão wizard, unânime na causa-raiz). Atualiza/supera o D-066. Pendências: backfill de especialidade na sync; reclassificação dos ~3.900 em Clínico Geral. |
| **D-066** | **Médicos & Escala vira um WIZARD por etapas** (muita informação numa tela só): (1) Localizar o doutor; (2) Cadastro do perfil médico (completo, editável; **remove o selo "importado da Teleconsulta"**); (3) Escala (vigência + especialidade); (4) Disponibilidade adicional. **O conceito "blocos" some da UI** — vira os **períodos internos** da escala (manhã/tarde com almoço). | 2026-06-15 | Confirmado por Alessandro ("esquema de etapas… muita informação"; "não entendi esse blocos"). |
| **D-065** | **Disponibilidade adicional (refina D-053):** só pode ser lançada sobre uma **escala fixa EXISTENTE/ativa** do médico. Ao lançar horas, o **valor é obrigatório**; vem como **default = valor/h adicional do cadastro** do médico, mas é **editável** (decisão da Demandas Médicas, caso a caso). | 2026-06-15 | Confirmado por Alessandro. |
| **D-064** | **Cadastro do médico = MÚLTIPLAS especialidades, com RQE POR especialidade (modelar AGORA).** O médico passa a ter N especialidades, cada uma com seu próprio RQE. (Operacionalmente atua em 1 por vez — ver D-063.) Schema: nova `doctor_specialties(doctor_id, specialty_id, rqe)`. | 2026-06-15 | Confirmado por Alessandro ("sim, agora"; "RQE por especialidade"). |
| **D-063** | **ESCALA = MENSAL, amarrada a UMA especialidade, só UMA ATIVA por médico, com HISTÓRICO/rastreabilidade.** Vigência por período (default **hoje → fim do mês**). "Quase não muda, mas quando muda guarda histórico": versiona — a anterior é arquivada, nada se perde. **Hoje 1 especialidade por vez ⇒ 1 escala ativa** (troca de especialidade = futuro). A escala **contém seus períodos** (os antigos "blocos"). Schema: nova `schedules(doctor_id, specialty_id, start_date, end_date, status, created_at)`; `schedule_blocks` ganha `schedule_id`. | 2026-06-15 | Confirmado por Alessandro. Refina D-051/D-022 (escala). Atende rastreabilidade (LGPD/auditoria). |
| **D-062** | **IA "CADASTROS" com submenu por PERFIL; "Estados & Unidades" OCULTO na Fase 1.** A gestão de usuários vira uma área **"Cadastros"** com submenus por perfil — **Médicos** (o cadastro mais completo: dono + escala), Administradores, Gestores Solicitantes, Gestores Regionais. "Médicos & Escala" passa a ser o submenu **Médicos**. (Médico é DADO, não loga — D-010 — mas tem o cadastro mais rico.) **"Estados & Unidades" foi ocultado** na Fase 1 (era reference das Fases 2–4). | 2026-06-15 | Confirmado por Alessandro (2 respostas: "área Cadastros com submenu por perfil"; "ocultar Estados & Unidades"). **Estruturação do menu Cadastros = pendente** (esta rodada ocultou Estados&Unidades e construiu o cadastro-dono; o submenu por perfil é o próximo passo). |
| **D-061** | **CADASTRO-DONO do médico na tela-centro (Médicos & Escala) — D-055 materializado.** A tela ganhou a seção **"Cadastro do médico"**: name/CRM read-only (vêm da TC), + campos que NASCEM aqui — RQE, CPF, nascimento, telefone, e-mail, **valor/h fixa e adicional** (D-053), especialidade editável. Backend: **GET /api/doctors/{id}** (detalhe) + **PUT /api/doctors/{id}** (atualiza só os campos-dono; name/crm intactos; papel admin/demandas). Colunas já existiam (migration 001). Avisa o que falta p/ faturamento (RQE/CPF/valor fixo). | 2026-06-15 | Confirmado por Alessandro ("complemento de informações"; "essa tela é nosso centro"). Round-trip GET/PUT validado em médico real. |
| **D-060** | **REMOÇÃO DOS MOCKS NA RAIZ — só dados de produção (refina D-058).** Os seeds de demonstração foram **estripados das migrations**: 002 mantém só REFERÊNCIA (health_centers, units, specialties); **003 esvaziado** (era ~300 doutores mock + pacientes + pools/solicitações/reservas/agendamentos das Fases 2–4); 005 sem linhas demo. **Médicos vêm 100% da SYNC de produção** (D-056), não de seed. Dados transacionais nascem do uso. Banco recriado limpo (4.523 médicos reais, 0 mock, 0 transação demo) e **criação de escala validada num médico real** (POST 201). **Consequência:** todo `down -v` exige re-rodar a sync (manual hoje; vira job — D-056). | 2026-06-15 | Confirmado por Alessandro ("remover tudo que é mock, fazer com dados de produção; médico é só leitura"). Reference (estados/unidades/especialidades) e personas seguem semeados. |
| **D-059** | **Persona "demandas" RELABEL → "Demandas Médicas"** (atualiza D-048); é a **dona da 1ª entrega** (Fase 1: cadastro/gestão de médicos + escala). **A chave de papel no código continua `demandas`** (só muda o rótulo de UI e a descrição). NÃO é um 5º papel novo. | 2026-06-15 | Confirmado por Alessandro. Mexe só em `ROLE_LABEL`/`ROLE_DESC` (front). RBAC inalterado. |
| **D-058** | **1ª ENTREGA FOCADA NA FASE 1 — por OCULTAÇÃO/GATE, não recriação.** Em vez de recriar o sistema só com a Fase 1, **restringe-se nav + rotas** às telas da Fase 1 (Visão geral, **Médicos & Escala**, Estados & Unidades, Usuários); as telas das Fases 2–4 (Solicitações→Disponibilização→Assunção→Agendamento) **ficam no código, ocultas**. Flag única `PHASE1_FOCUS` em `app/src/app/nav.tsx` (reversível: `false` reativa tudo). `ProtectedRoute` também bloqueia rota digitada fora da Fase 1. **Nada foi removido.** | 2026-06-15 | Confirmado por Alessandro ("ocultar as outras telas… focar na Fase 1"). Preserva auth/docker/camada de dados/carga. Recriar do zero foi descartado (caro/arriscado). |
| **D-056-exec** | **PRIMEIRA CARGA EXECUTADA (2026-06-15):** importados **4.523 médicos ATIVOS** da TC (prod RO) → tabela `doctors` (`source='teleconsulta-sync'`), com **nome (title-cased), CRM (96%), especialidade** mapeada pelo enum real; **4.523 vínculos** em `doctor_external_refs` (system=`teleconsulta`, external_id=id da TC). Catálogo completado p/ FK (migration **007**, +9 especialidades reais, lideradas por **Clínico Geral** = 86%). **306 mocks preservados** (não quebrar escala/agendamentos demo) — purgar é follow-up. 182 sem CRM (campo vazio, preenche aqui). Temp com PII removido. | 2026-06-15 | Materializa D-056. Pull direto do CORE (nome via `users→persons`; CRM via `doctor_profile_licenses`). Re-rodável (upsert por id). |
| **D-057** | **CONVENÇÃO DE CÓDIGO = INGLÊS + snake_case (Postgres) — MIGRAR TUDO AGORA.** Tabelas e colunas do banco em **inglês `snake_case`** (`doctors.name`, `specialties`, `units`, `schedule_blocks`, `additional_schedules`…); **código em inglês**. O schema/protótipo atual (português: `nome`, `especialidades`, `unidades`, `blocos_escala`, `escala_adicional`…) é **renomeado por completo** nas 3 camadas (migrations SQL, DTOs/repos/endpoints .NET, store/interfaces React). **Vínculo do médico por destino = tabela `doctor_external_refs`** (`doctor_id`, `system` [teleconsulta|telediagnostico|…], `external_id`, `status`) — ids distintos por destino, **extensível a sistemas futuros** (não só TC/TD). | 2026-06-15 | Confirmado por Alessandro ("nomes das tabelas e código em inglês, snake_case"; "tabela de vínculos por destino, ids diferentes, preparada p/ outros sistemas além de TC/TD"; "migrar tudo para inglês agora"). Refatoração mecânica grande — fazer com verificação (app tem de continuar subindo). Glossário PT→EN em `docs/discovery/glossary.md`. |
| **D-056** | **IMPORTAÇÃO = JOB de SYNC RECORRENTE da Teleconsulta até a virada de chave (refina D-054/D-055).** Enquanto o cadastro/edição/exclusão do médico **ainda existir na TC**, rodamos um **job recorrente** que faz **upsert por `external_id`** dos médicos **ATIVOS** (`deleted_at IS NULL`) na nossa base — mantém o espelho atualizado, não é carga única. **Quando virar a chave**, o CRUD passa a ser **só no nosso sistema**, o job para, e a direção se inverte (nós→TC via API de parceiro). O pull é quase todo no **CORE** (id, nome 100%, CRM 96%, especialização, CPF, nascimento) + **email do AUTH**; **RQE/valores nascem aqui**. Tudo **RO** (nada escreve na TC). | 2026-06-15 | Confirmado por Alessandro ("um jobzinho que fica fazendo a importação enquanto não removemos a funcionalidade de lá; quando virar a chave, fica por aqui"). Caveat operacional: RO depende de lease Vault 24h + proxies (caem em reboot) — `scripts/db/prod-up.sh`. Para o protótipo o job pode ser on-demand/agendado enquanto o acesso está de pé. |
| **D-055** | **REPOSICIONAMENTO: nosso sistema é o CADASTRO-MESTRE (dono) do MÉDICO. O médico NASCE aqui e é PROVISIONADO para os produtos a jusante (Teleconsulta e/ou Telediagnóstico).** Resolve a Q-rename: (a) o produto é **reposicionado** de "planejamento de capacidade oferta×demanda / Agenda Fixa" para **gestão de médicos** com o **médico como entidade central**; (b) o **cadastro-dono do médico** (nome, CRM, RQE, CPF, contato, especialidade, valores fixo/adicional) entra **já na 1ª entrega** (não é mais "desejo futuro" do D-054); (c) **Telediagnóstico NÃO é "origem/modalidade" do médico** — é um **DESTINO**: o cadastro nasce no nosso sistema e dali é **transferido/provisionado** para o produto onde o médico vai atuar (TC e/ou Telediagnóstico), cada destino amarrado por seu **`external_id`** próprio. O legado **não tem cadastro de médico confiável** (CRM ~2%, nome ~3% preenchidos — ver `docs/discovery/11-doutor-schema-legado.md`), o que **justifica** sermos a fonte. Para os ~4.500 médicos já existentes na TC: **reconciliação única** (importar/casar por email+id → daí em diante o dono é o nosso sistema). | 2026-06-15 | Confirmado por Alessandro (3 respostas: "reposiciona p/ gestão de médicos"; "cadastro-dono na 1ª entrega"; "telediagnóstico não é origem — o cadastro nasce aqui e é transferido"). **Amplia o escopo da 1ª entrega além da Fase 1 escala (D-052).** **Inverte a direção do D-054**: D-054 era "snapshot RO read-only (TC→nós)"; agora a visão é **nós→TC/Telediag (escrita/provisionamento)** — o snapshot RO vira só a **reconciliação inicial** dos existentes. **Escrita na prod da TC continua proibida via DB direto** — provisionamento será via API/sync de parceiro (a desenhar). **NOME DO PRODUTO = "Doctor-Hub"** (confirmado por Alessandro 2026-06-15; "hub" reflete o papel de ponto central que distribui o médico aos destinos TC/Telediagnóstico). Ressalvas registradas (genérico/colisão de marca) ficam p/ eventual validação de domínio/marca antes de marca pública. |
| **D-030** | **STACK ESCOLHIDA (resolve/encerra D-001): protótipo FUNCIONAL = React + Vite + TypeScript (SPA), rodando local.** Auth & autorização MOCK: usuários fictícios por papel, login real no front, **autorização por perfil** (rotas/telas por papel), **dados fake em memória**. O perfil logado dirige a UI dinamicamente (acaba o topbar hardcoded). Reaproveita design/tokens.css + design/components. Backend/integração real = depois. App vive em `app/`. | 2026-06-14 | O usuário pediu um protótipo funcional que reflita o sistema final. Vira a Fase 1 (construção). Ver design/BUILD-PROGRESS.md. |
| **D-031** | **MODELO DE NÍVEIS (núcleo oferta×demanda, 3 camadas).** (1) **Solicitação** e (2) **Disponibilização** acontecem no nível do **ESTADO (cliente)** — ex.: "1000 vagas de Cardio pro Amazonas". A disponibilização cria um **POOL por estado**. (3) O **Gestor da unidade** enxerga **só o pool do seu estado** e **ASSUME uma fatia** para sua unidade (reserva, ainda sem doutor/paciente). (4) O **mesmo Gestor** materializa o slot (doutor + paciente) → vira agendamento → vai à TC. | 2026-06-15 | Confirmado por Alessandro (descreve o fluxo manual real, hoje em planilha). **Reescreve o protótipo** (que tratava solicitação/disponibilização por HC). Resolve "slot→HC", "quem reserva", "quem agenda". |
| **D-032** | **Disponibilização = MISTO (refina D-005):** o sistema **SUGERE** a capacidade a partir da escala dos médicos e o operador (Demandas) pode **AJUSTAR** para mais/menos (retornos, extras, encaixes). Não é número 100% livre nem 100% travado. | 2026-06-15 | Confirmado por Alessandro. Casa com D-005 (estoque misto auto+manual). |
| **D-033** | **Granularidade do estoque = HÍBRIDO (atualiza D-028):** o **POOL** é uma **CONTAGEM abstrata** de capacidade no nível do estado; o **HORÁRIO concreto** (dia/hora/médico) é **MATERIALIZADO da escala** disponível no momento em que o Gestor assume/agenda. Contagem em cima, concreto embaixo. | 2026-06-15 | Confirmado por Alessandro. **Supersede a parte de D-028** que dizia "horário concreto, não contagem". |
| **D-034** | **Escolha do doutor no vínculo = SUGERE + PERMITE TROCAR (confirma D-011):** ao materializar o slot, o sistema sugere o médico (preferencial do paciente → fallback de especialidade) e o Gestor pode trocar. | 2026-06-15 | Confirmado por Alessandro. Mantém D-011 com override manual. |
| **D-035** | **Status da solicitação = 3 estados simples:** **Enviada → Atendida parcial → Atendida**, **DERIVADO** automaticamente da relação disponibilizado×solicitado. Sem 'rascunho' nem 'em_analise'. | 2026-06-15 | Confirmado por Alessandro. Remove os estados que o seed tinha mas a UI nunca alcançava. |
| **D-036** | **Limite da reserva do Gestor = saldo do pool + TETO da unidade.** O Gestor só assume até o que sobrou do pool do estado **E** até a cota da sua unidade. | 2026-06-15 | Confirmado por Alessandro. Introduz o conceito de cota por unidade (ver D-037). |
| **D-037** | **Cota (teto) da unidade = CADASTRO FIXO por unidade** (capacidade da UBS), independente da disponibilização do mês. | 2026-06-15 | Confirmado por Alessandro. Vira campo de cadastro da unidade (HC). |
| **D-038** | **Escopo do Gestor = DOIS eixos válidos:** pertence a uma **UNIDADE** (HC — onde tem cota e assume) **e** enxerga o **POOL do seu ESTADO** (cliente). `scopeClienteId` + `scopeHcId` **não** são dead-data. | 2026-06-15 | Confirmado por Alessandro (decorre de D-031/D-037). Resolve a dúvida "escopo do gestor = 1 HC?". |
| **D-039** | **Escala definida por DURAÇÃO em minutos por consulta** (ex.: 12/15/26 min), **não** por consultas/hora. Consultas/hora vira só aproximação derivada/exibida. Nº de vagas = janela ÷ duração. | 2026-06-15 | Confirmado por Alessandro. **Atualiza D-028** (que tomava consultas/hora como input). Inverte o input da escala no protótipo. |
| **D-040** | **Borda dos campos de formulário REFORÇADA** para atingir **WCAG 1.4.11** (contraste ≥3:1 em componentes). | 2026-06-15 | Confirmado por Alessandro. Reforça a meta D-017 (AA). Ajuste de tokens. |
| **D-041** | **VOCABULÁRIO/HIERARQUIA corrigido (atualiza D-018, D-031):** **HealthCenter = um CLIENTE = um ESTADO** (Piauí, Amazonas, Amapá, Alagoas, IASEP-PARÁ). Tem ID próprio; **nunca contém dois estados**. **Abaixo do HC há UNIDADES/GRUPOS** ligados ao **CNES** (presencial = endereço; remoto = CNES do núcleo de telessaúde do estado), definidos pelas tabelas **`profile_tags` / `patient_profile_tags`** da Teleconsulta. **Uma unidade nunca pertence a dois estados.** HealthCenter é flexível (pode representar estado, cidade ou clínica) — **1ª entrega foca em HCs que representam ESTADOS**; outros modelos ficam abertos p/ depois. | 2026-06-15 | Confirmado por Alessandro. **Renomeia o modelo do protótipo:** nosso "Cliente" → **HealthCenter**; nosso "HC/unidade" → **Unidade (profile_tag)**. Modelo da TC verificado em `docs/discovery/08-healthcenter-unidades.md` (tabelas `health_centers`, `profile_tags`, `patient_profile_tags`, enum `ProfileTagType`, endpoint `/integration/profile-tags/by-cnes`). |
| **D-042** | **Agendamento é DESACOPLADO da solicitação de vagas** — dois fluxos separados. A reserva do gestor pode ser só *quantidade* (sem doutor/paciente) e o agendamento vem depois (ou nem vem, se só reservou). | 2026-06-15 | Confirmado por Alessandro. Reforça D-027/D-029: agendar ≠ reservar. |
| **D-043** | **Sem passo de aprovação explícito:** o ato de **disponibilizar já libera o pool** pro estado. Não há etapa de "aprovação" separada antes do saldo existir. | 2026-06-15 | Confirmado por Alessandro. Simplifica o fluxo de D-031. |
| **D-044** | **Reserva do gestor tem dois modos:** (a) só **reservar quantidade** do pool p/ sua unidade (sem doutor/paciente), ou (b) já **agendar** (paciente + doutor num horário). Reserva ≠ agendamento (ver D-042). | 2026-06-15 | Confirmado por Alessandro. |
| **D-045** | **Disponibilidade na 1ª entrega = HÍBRIDO (refina D-032/D-033):** o sistema **calcula o que der das escalas ativas dos doutores** e o operador **completa/ajusta** o restante à mão. Derivação 100% automática da escala = evolução futura. | 2026-06-15 | Confirmado por Alessandro ("o ideal é a disponibilidade vir da qtd de doutores com escala ativa, mas não dá pra ter todo o processo já"). |
| **D-046** | **Teto da unidade = capacidade de atendimento DIÁRIO** da unidade (refina/precisa D-037). | 2026-06-15 | Confirmado por Alessandro. A cota fixa cadastrada por unidade é expressa como capacidade diária. |
| **D-052** | **ROADMAP DA DIRETORIA — 4 fases; a 1ª ENTREGA = FASE 1 (Escala médica)** (atualiza/estreita D-027). Fases: **(1) Escala médica** [perfil Demandas] — busca doutor na TC + cadastro local (CRM/RQE/contato/valores) + **escala fixa + adicional** → **cálculo de capacidade por especialidade** (previsibilidade/gráficos); **(2) Solicitações** [Gestor HC + Demandas] — pedido por especialidade → disponibilização (pool); **(3) Agendamento** [Gestor] — reserva + paciente+doutor → integra TC; **(4) Remanejamento** [Demandas] — slots ociosos (48h) realocados entre HCs (depois automático via IA). | 2026-06-15 | Confirmado pela diretoria (4 diagramas, 2026-06-15). O que já construímos (pool/reserva/agendamento) mapeia nas Fases 2–4 e segue válido — só a **1ª entrega foca a Fase 1**. **D-027 fica superseded** quanto ao escopo da 1ª entrega. |
| **D-054** | **Doutores reais via SNAPSHOT da Teleconsulta (prod, RO) + `external_id`.** Tira os mocks: puxamos (uma vez, pela ferramenta RO sancionada) os doutores reais (nome/CRM/especialidade) pra nossa tabela `doctors`, guardando o **`external_id`** = id do `DoctorProfile` da TC (auth-server). A busca passa a ser real e estável (na nossa base). **Acesso = BANCO DIRETO (RO), não a partner-API** — escolha do Alessandro p/ encurtar o trabalho no protótipo. Modo **SNAPSHOT/ETL** (carga única na nossa base), NÃO conexão ao vivo a cada busca (essa seria frágil: lease Vault 24h + proxies caem em reboot + 2 bancos auth/core). Caveats aceitos: DB não tem contrato (schema da TC pode mudar e quebrar a query); a partner-API (`GET /integration/doctor`, D-021) e a escrita (visão "doutor nasce aqui, TC consome") ficam p/ depois (escrita na prod da TC só via sync/API, nunca DB). | 2026-06-15 | Confirmado por Alessandro ("snapshot real + external_id"). Estrutural feito (coluna `external_id` + DTO). **Pull pendente**: requer religar o acesso RO de prod (proxies off pós-reboot). |
| **D-053** | **Disponibilidade do médico = ESCALA FIXA + ESCALA ADICIONAL.** **Fixa** = baseline (previsibilidade/planejamento). **Adicional** = disponibilidade **excepcional POR DIA específico (datada) + período(s) extra** — cobre faltas/lacunas, **sem alterar a fixa**. **Capacidade do dia = fixa + adicional**, mas a **previsibilidade/baseline usa SÓ a fixa** (o adicional é "extra pontual", marcado à parte). **Faturamento: a hora ADICIONAL tem TAXA PRÓPRIA** (distinta da fixa) — modelar desde a Fase 1. Cadastro do doutor ganha CRM, RQE, endereço/contato e **valores (hora/consulta) fixo + adicional**. | 2026-06-15 | Confirmado por Alessandro (pós-diretoria). Refina D-039/D-051 (escala). Substitui a ideia anterior de "retornos/extras" genéricos por um modelo explícito fixa+adicional. |
| **D-051** | **Escala suporta MÚLTIPLOS PERÍODOS por dia (estilo SAP: início→retorno, início→retorno).** O **vão entre períodos (ex.: horário de almoço/pausa) NÃO gera slots**. Modelado como **múltiplos blocos não-sobrepostos** no mesmo dia (o modelo `blocos_escala` não muda; o builder cria 1 bloco por período). Preset "Integral" passa a ser **"Integral c/ almoço (08–12, 13–17)"**. | 2026-06-15 | Confirmado por Alessandro ("cadê o horário de almoço? igual ao SAP"). **Resolve** a parte "intervalos (almoço)" da 🟡 de fórmula de capacidade. Feriados continuam 🟡. |
| **D-050** | **Nosso sistema é a FONTE DA VERDADE da relação Especialidade × HealthCenter** (a montante do legado). A relação é **modelada e gerida no NOSSO banco** (tela de admin "Especialidades por Estado"), seedada com o dado real extraído da TC (ver `docs/discovery/10-especialidades-por-hc.md`); todo o app lê dela (ex.: a tela de Solicitação mostra só as especialidades do estado). **O "refletir no legado" (TC `health_center_specialties`) é uma INTEGRAÇÃO de ESCRITA** que NÃO existe ainda — só temos RO; e por D-002 é via API de parceiro, não DB direto. **Mecanismo de reflexão (confirmado): a TC LÊ de nós** — expomos `GET /integration/hc-especialidades` e a TC consome; **não** escrevemos no banco do legado (coerente com D-002, e só temos RO). **Catálogo = todas as especialidades históricas** de cada estado (do dado real), ajustável na tela de admin. | 2026-06-15 | Confirmado por Alessandro (visão "ditar a regra"; "TC lê de nós"; "todas as históricas"). Chave canônica = `InternalSpecializationId` (enum `DoctorSpecializationType`). |
| **D-049** | **Backend do protótipo funcional = .NET 10 + Dapper + PostgreSQL, TUDO EM DOCKER** (alinhado ao stack da Teleconsulta, que é C#/.NET). `docker compose` com 3 serviços: **`db`** (postgres:16-alpine, volume), **`api`** (.NET 10, multi-stage sdk→aspnet), **`web`** (front buildado servido por nginx, proxy `/api`→api). API simples, **auth real** (hash de senha + JWT). **Seed com carga REALISTA** (~300 doutores, 60–100 usuários, dezenas de unidades, centenas de solicitações/reservas/agendamentos). O front migra de `localStorage` → **API**, mantendo os contratos `useData()`/`useAuth()`. **Ainda é PROTÓTIPO** (sem integração real com a TC, sem produção). Monorepo: `app/` (front) + **`server/`** (novo) + `docker-compose.yml`. | 2026-06-15 | Confirmado por Alessandro ("tudo em docker"). Atualiza a parte da D-030 ("dados fake em memória / backend depois"). Cred de DB fica em env do compose (`.env`, dev local — não é segredo de produção). O Postgres local `saude_demandas` que preparei na sessão fica **sem uso** (o db roda em container). |
| **D-048** | **Nomenclatura das personas (rótulos de UI; atualiza D-008):** "Solicitante" → **"Gestor Solicitante"** (nível ESTADO/HealthCenter, registra a demanda); "Gestor local" → **"Gestor"** (nível UNIDADE, reserva do pool e agenda). Admin e Demandas mantêm. As **chaves de papel no código continuam `solicitante` e `gestor`** (só o label muda). Personas reais nomeadas: **Admin = Renata Troncoso** (renata@portaltelemedicina.com.br), **Demandas = Yannka Lins** (y.lins@portaltelemedicina.com.br). Demo passa a ter **1 Gestor Solicitante + 1 Gestor por estado** (Piauí, Amazonas, Amapá, Alagoas, IASEP-Pará). | 2026-06-15 | Confirmado por Alessandro. Nomes/e-mails dos 10 gestores estaduais são **dado de demo** (e-mails gov fictícios, ajustáveis). |
| **D-047** | **Remanejamento (precisa D-013):** vagas **reservadas pro estado**, **não utilizadas**, **vencendo em 48h**, **sem paciente/doutor alocado** → **Demandas** pode **realocar** esses slots. | 2026-06-15 | Confirmado por Alessandro. Confirma a janela 48h e o critério "slot ocioso perto do vencimento". |
| **D-001** | ~~Não escolher stack até a Fase 1~~ **SUPERSEDED por D-030** (o usuário optou por construir o protótipo funcional). | 2026-06-13 | Ver `CLAUDE.md`. |
| **D-002** | O sistema é um **produto/repo SEPARADO** que integra com a Teleconsulta como **parceiro** (via `X-API-KEY`), não um serviço dentro do monorepo da TC. | 2026-06-14 | Liberdade total de stack. Precisa de novo `PartnerType` + API-key emitida pela equipe da TC. Duplica infra/CI. |
| **D-003** | A **alocação de médico é NOSSA**: o novo sistema decide o médico e o envia em `preference_of_doctor_id`. A TC **respeita** o que está em `external_appointments`. | 2026-06-14 | Resolve a sobreposição com `ptm-matching-api` da TC (que só agiria sem preferência). O vínculo médico↔paciente é o `preference_of_doctor_id`. |
| **D-004** | **Paciente é entidade deste sistema** — montamos o agendamento completo (médico+paciente+especialidade+HC) e inserimos na TC. | 2026-06-13 | Traz LGPD/dado sensível para a v1. Precisa resolver `patient_id` da TC (lookup CPF/CNS). |
| **D-005** | **Estoque misto**: base calculada da escala (dias×horário×consultas/hora×período) + ajuste manual ("retornos/extras", com auditoria). | 2026-06-13 | Falta a fórmula exata de derivação. |
| **D-006** | **Remanejamento por regras determinísticas** na v1 (sem ML). Auditável. | 2026-06-13 | Falta definir QUAIS regras/prioridades. |
| **D-007** | **NÃO construir app com Claude Code embarcado.** A interação com IA é via Claude Code interativo (terminal, coberto pela assinatura). O "portal visual" é só documentação de prateleira (Obsidian/MkDocs) sobre as specs. | 2026-06-14 | Motivo: mudança de billing de 15/06/2026 (Agent SDK a preço de API) tirou a vantagem de embarcar. Reduz muito o escopo e a manutenção. |
| **D-008** | **Mapa de papéis:** (a) **Admin/Demandas** = operadores internos (PTM); (b) **Solicitante = Secretário de Saúde estadual** abre a solicitação; (c) **Gestor = gestor local de unidade/HC** (~1 por cidade) assume os slots da sua unidade. **Só esses 3 logam.** | 2026-06-14 | Base do RBAC. Escopo de dados por unidade/estado é provável (a confirmar). |
| **D-009** | **Paciente é associado à vaga NO MOMENTO em que o Gestor local assume o slot** — ele seleciona o paciente ali. | 2026-06-14 | Refina D-004. O cadastro/origem da lista de pacientes do gestor ainda precisa ser detalhado. |
| **D-010** | **Doutor e Paciente são DADOS, não usuários** — não fazem login no sistema. | 2026-06-14 | Médico é cadastrado (escala) por um operador; paciente é selecionado pelo gestor. |
| **D-011** | **Lógica do médico preferencial:** em **retorno**, o último doutor que atendeu vira preferencial; senão o gestor escolhe um doutor preferencial. O preferencial pode não estar disponível no dia → atendimento por **outro doutor da mesma especialidade**. | 2026-06-14 | Casa com `preference_of_doctor_id` + fallback de especialidade da TC (ver D-003). |
| **D-012** | **A lista de pacientes vem da Teleconsulta** — já existe endpoint que expõe pacientes por health center. O Gestor seleciona dali; o `patient_id` da TC já vem resolvido. | 2026-06-14 | Refina D-004/D-009. Reduz exposição LGPD: o master de paciente é da TC, não nosso. Localizar o endpoint exato no repo da TC. |
| **D-013** | **Remanejamento v1:** gatilho por **janela configurável** (ex.: 24h/48h após disponibilização) sobre slots não assumidos; critério = **demanda não atendida** (HC com saldo negativo); **não precisa ser automático na v1** (operador pode rodar), mas a janela é configurável. | 2026-06-14 | Motivação econômica: o médico já foi pago — não desperdiçar capacidade. Detalha D-006. |
| **D-027** | **CORRIGE D-023 — a Primeira Entrega vai ATÉ o agendamento na Teleconsulta.** Fluxo completo: solicitação de especialidades → montagem da escala → reserva de slots pelas unidades → agendamento (doutor + paciente no slot) → envio à TC. "Vender depois" = avançado: elegibilidade, produtividade médica, monitor proativo, remanejamento automático. | 2026-06-14 | NÃO é mais "só a base". D-023 fica SUPERSEDED. Nada foi entregue ainda — é um protótipo único em refinamento (sem 'v1/v2/legado'). |
| **D-028** | ⚠️ **ATUALIZADA por D-033 + D-039.** ~~Slots são CONCRETOS no estoque; Duração = 60÷consultas-por-hora~~. Agora: o **estoque é contagem** (D-033) e a escala se materializa em slots concretos só na assunção/agendamento; **input da escala é duração em minutos** (D-039), não consultas/hora. A escala do doutor segue **gerando** os slots concretos quando materializados. | 2026-06-14 (atual. 2026-06-15) | Granularidade migrou de "horário concreto no estoque" para "contagem em cima, concreto embaixo". |
| **D-029** | **Agendamento = atribuir 1 DOUTOR + 1 PACIENTE a um slot concreto.** Ex.: Cardiologia · terça · 08:00–08:20 → Dr. Fernando atende paciente Thiago → enviado à TC. | 2026-06-14 | Coerente com D-003/D-011 (alocação nossa, médico preferencial). 🟡 confirmar quem faz: gestor da unidade ou operador. |
| **D-026** | **A escala é NOSSA (própria)** — criada e gerida no novo sistema (gera o estoque). Da TC só **buscamos o profissional** (`GET /integration/doctor`). NÃO usamos a ScheduleGroup/AvailabilityRule da TC como fonte (a TC tem a dela, interna; nós temos a nossa). | 2026-06-14 | Resolve a fronteira de 07-escala. Confirma a Médicos & Escala v2. Aceita duplicar o conceito de escala. |
| **D-021** | **Médico vem da Teleconsulta (busca), não cadastro livre** — buscar o profissional existente na TC → selecionar → criar a escala. | 2026-06-14 | 🔴 confirmar endpoint de busca de profissional na TC. Ver `discovery/07`. |
| **D-022** | **Arquitetura da escala robusta:** presets (meio período/integral/madrugada/personalizado); múltiplos blocos por médico **sem sobreposição**; bloco pode cruzar a meia-noite; vigência com/sem fim; **ativar/inativar total ou parcial**; **timeline de versões** + **rastreabilidade (histórico)**. Modelo já preparado para **produtividade** futura (previsto×realizado, videochamada, transcrição, auditoria por IA médica). | 2026-06-14 | UI da Entrega 1 mostra o básico; o MODELO já comporta o resto (vender depois). Ver `discovery/07`. |
| **D-023** | **Re-escopo Entrega 1 = a BASE** (alinhada ao dashboard do ClickUp): Acesso/credenciais/perfis, Gestão de usuários e credenciais, Gestão de solicitações de demandas, Médicos & Escala (capacidade), Visão por perfil. **Agendamento, elegibilidade, integração FULL com a TC, remanejamento auto e produtividade = entregas futuras.** | 2026-06-14 | 🔴 confirmar fronteira contra o board do ClickUp. Ver `product/09`. |
| **D-024** | **Dashboards operacionais e por perfil:** responder solicitado × disponibilizado × saldo × **em-risco (remanejar)**, agrupado **por especialidade** e **por health center**; home específica por persona. | 2026-06-14 | A "Visão geral" atual é fraca/só-admin. Ver `product/09`. |
| **D-025** | **Apresentações por FASE + PERSONA no Figma** — usar Flows (1 `flowStartingPoint` por persona, começando no Login); **login interativo** com fluxo feliz e infeliz (usuário/senha inválida, encenado). | 2026-06-14 | Cada apresentação mostra só a jornada de uma persona. Ver `product/09`. |
| **D-018** | **Clientes podem ser público ou privado.** Público = estado/órgão (ex.: Piauí); somos prestadores p/ uma empresa com vínculo de governo. Privado = clínica ou plano de saúde. Cada cliente agrupa HCs; visões por HC e consolidado (todos os clientes). | 2026-06-14 | Novo conceito "Cliente" acima de HC. Ver `docs/product/07-fases-entrega.md`. |
| **D-019** | **Problema-núcleo = substituir o controle manual em Excel** (`agenda-operacional-*.xlsx`, mantido por 1-2 pessoas horas/dia). A Fase 1 (~3 sem) entrega o "cockpit de planejamento" que tira isso do operacional. | 2026-06-14 | Planilhas reais em ~/Downloads. Agente analisando → `docs/discovery/05-processo-manual-excel.md`. |
| **D-020** | **Entrega por fases com apresentação ISOLADA no Figma** — cada fase tem um board só com as suas telas (clareza ao stakeholder). Fase 0=Figma, Fase 1=cockpit/sair-do-Excel, Fase 2=execução+integração TC. | 2026-06-14 | Atende a preocupação de "entendimento do humano". |
| **D-015** | **Responsividade total:** máxima fidelidade cobre desktop + tablet + mobile, todos os papéis (Gestor é o caso mobile prioritário). | 2026-06-14 | Breakpoints sm640/md768/lg1024/xl1280/2xl1440. Ver `design/design-system.md`. |
| **D-016** | **Identidade neutra por agora, tokenizada** — slate + acento azul; a marca (PTM ou nova) entra depois só remapeando tokens. | 2026-06-14 | Sem retrabalho de telas. |
| **D-017** | **Acessibilidade WCAG 2.2 AA** como meta do design system. | 2026-06-14 | Contraste/foco/alvos verificados nos tokens. |
| **D-014** | **Modelo de entrega:** escopo = **Tier 2**, entregue de forma **incremental por TELA** com **checkpoint semanal** (diretoria pensa em telas). **Protótipo Figma na semana 1** (diretoria vê o produto), depois tela-a-tela vira software real. **Tier 1 é um marco interno (M4)** já demonstrável/faturável dentro da jornada do Tier 2. Parâmetros: R$180/h, 10h/sem, AI coding. | 2026-06-14 | Ver `docs/product/06-roadmap-telas.md`. Reconcilia "quero Tier 2" + "valor cedo": Tier 1 entrega no meio do caminho. |

| **D-074** | **Marca aplicada (executa D-016): "admin modernizado" da Portal Telemedicina.** Paleta: navy `#054671` (primária/sidebar/títulos) + azul `#0073BD` (acento: links/nav/info/2ª ação) + **laranja `#B85410` SÓ em CTA de ação** (1 por tela). Laranja vivo `#ED7225` só decorativo. Tipografia híbrida: **Montserrat** (títulos) + **Inter** (corpo/tabelas). Status seguem convencionais. **WCAG AA verificado** (laranja `#ED7225` com texto branco reprova 2.99 → usa-se fill `#B85410` 4.87). Design system próprio sobre os tokens existentes (sem UI kit externo). Processo **Figma → código**. | 2026-06-16 | Fonte da verdade: `docs/design/design-system.md`. Figma: coleção "Tokens" brandizada (50 vars) + página "Design System" no arquivo `NCMcYURZgrHH36f9DTk7di`. Falta espelhar no código (`tokens.css` + Montserrat). |

## Decisões de método (já materializadas no repo)
- **M-001** Spec como fonte da verdade (`specs/`), ciclo draft→specified→tested→implemented.
- **M-002** Zero inferência de regra de negócio; dúvida vira pergunta aberta (Diretriz Suprema, `CLAUDE.md`).
- **M-003** Enforcement por hooks spec-first/TDD (desenho em `docs/method/spec-first-hook.md`).
- **M-004** SDD+TDD alinhados ao que a própria Teleconsulta já pratica (monorepo com `/specs`).

### D-157 — Fluxo assunção→agendamento: real até o TC; busca de paciente real POR ÚLTIMO (2026-07-05)
**Contexto:** Alessandro reafirmou o foco (assumir slot → paciente → agendamento) e pediu para deixar
a **busca de paciente REAL no Teleconsulta por último** — envolve dados reais do TC + secrets, e ele
quer estar **presente na máquina** pra analisar código e segredos (está remoto).
**Estado:** o fluxo JÁ existe ponta-a-ponta — `POST /agendamentos` assume vaga → paciente **por
INICIAIS** (guard LGPD) → cria agendamento → **para no TC** (`EnviadoTc=false`, DEP-TC-1). A tela
`assuncao` fecha ④+⑤ com gate de aceite (D-116).
**Gap (nada de mock):** as **vagas** da tela vêm de fixture (`./data`), não da capacidade real.
**Decisão:** próximo passo real = **derivar as vagas da capacidade REAL** (escalas×solicitações, o
modelo do painel de déficit), tornando o ciclo 100% real até o agendamento. A **busca de paciente
real no TC fica adiada** (PROVISÓRIO: paciente por iniciais manual) até o Alessandro estar presente.

### D-152 ✅ CONFIRMADO (2026-07-05) — "semana do mês" = n-ésima OCORRÊNCIA do dia
Alessandro cravou: "folgo na 2ª semana" = a **2ª ocorrência daquele dia** (2ª segunda = 13/jul/2026),
não semana-calendário. O código já implementava (`ocorrenciaNoMes = ceil(dia/7)`); removido o PROVISÓRIO.
Crítico para o cadastro REAL de escalas (capacidade correta).

### D-158 — Feature flags por TENANT (white-label), configurados pelo super-admin (2026-07-05)
Alessandro: diferenciar **clientes do doc hub (tenants)** dos **clientes dos clientes (HCs)** e
habilitar/desabilitar features **por tenant** (ex.: Portal → só teleatendimento + telediagnóstico).
**(a)** Sistema GENÉRICO de flags (tipos de serviço + módulos + features futuras), não hardcode.
**(b)** Configurado só pelo **super-admin do doc hub** (você). Design/spec: doc 32. Fase 1 = tenant +
tipos de serviço por tenant + tela de config. Fundação multi-tenant → também P-013.

### D-159 — Modelo canônico de atores & fluxo (2026-07-05)
Alessandro articulou o modelo completo: super-admin (cria tenants+features) · admin (tenant: cria
usuários e HCs, configura especialidades por HC) · demandas (escalas+médicos, usuário principal) ·
regulação (HC: solicita consultas, quem pede primeiro tem prioridade) · supervisor (HC+unidades:
agenda os slots). Fluxo: escala → analisa pedidos → recruta → disponibiliza p/ HC → HC aprova
liberação → supervisor agenda. Detalhe + deltas: discovery/02-roles.md. Vira spec por fatia.

### D-157 ✅ FEITO (2026-07-05) — assunção com vagas REAIS + demo blindada
`GET /api/vagas` deriva 944 vagas reais da capacidade (escalas), substituindo a fixture — o ciclo
fecha sem mock. + Error Boundary (fim da tela branca), banner "dados de demonstração" (sem mock
silencioso), Monitor de Integração fora da nav (DEP-TC-1). **2 itens PROVISÓRIO abertos (não inferidos,
p/ o build do D-159):** (a) vínculo vaga↔solicitação p/ o gate de aceite D-116 em vagas reais
(hoje solicitacaoId=null → vaga disponível); (b) mapeamento vaga↔unidade do gestor (gestor sem
clienteId → fail-closed vazio; a demo roda como Admin/Demandas=VeTudo). Busca de paciente real no TC
segue adiada (paciente por iniciais manual).

### D-160 — i18n multi-idioma (2026-07-05, registrado)
Detectar idioma + traduzir a UI (PT default · EN · ES · DE · …) quando houver tradução. Código em
inglês mantém-se. Framework próprio (chaves + arquivos). Detalhe: doc 28. Imediato feito: strings
soltas em inglês → PT ("Tenants & Features" → "Tenants e Funcionalidades").

### D-161 — White-label de identidade por tenant + tela de detalhe do tenant (2026-07-05, registrado)
Evolução de P-013: identidade visual por tenant (logo/cores/e-mail/telefone/CNPJ próprios; ocultar a
marca doc hub) — "separar tech de healthtech". 1ª fatia = tela de detalhe do tenant (CNPJ/endereço/
telefones/e-mails, não-CRUD). Direção registrada; não construir agora. Detalhe: doc 28.

### D-162 — Hierarquia de 4 níveis afiada + "Regulação"/"Supervisor" a renomear (2026-07-05)
Alessandro (homologação): o papel "regulação" NÃO é regulação — na prática é o **admin do cliente do
tenant**. Hierarquia real:
- **doc hub** (produto, Portal Tecnologia/Garbiati = super-admin)
- **TENANT** = cliente do doc hub (ex.: Portal Telemedicina) — tem contratos com estados, municípios,
  e privados (ex.: rede de academias). Papéis internos: admin (tenant), demandas (escalas+médicos).
- **CLIENTE do tenant** (o estado/município/academia = HC) — precisa de um **ADMIN próprio** (o atual
  "regulação"): cria os usuários do cliente (os "operadores"), **solicita atendimentos**, acompanha as
  solicitações, e **vê as especialidades contratadas** (futuro: preços por especialidade).
- **OPERADOR** do cliente (o atual "supervisor"/"gestor"): **realiza o agendamento** dos slots.
**Pendente:** renomear os papéis (regulação → admin-do-cliente; supervisor → operador) — decisão do
Alessandro entre 10 nomes propostos. Rename = refactor real (client role Keycloak + políticas + claims).
**UI relacionada:** "Meus dados" mostra "Órgão" (errado — assume governo; cliente pode ser academia) →
trocar por "Vínculo"/"Cliente". Logo do cliente ausente → liga com D-161 (identidade por tenant/cliente).

